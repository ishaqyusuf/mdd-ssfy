# Mobile Invoice Form

## Purpose

Tracks the Expo mobile invoice/quote form under `apps/expo-app/src/features/sales/invoice-form`.

## Current Architecture

- The form uses shared `@gnd/sales/sales-form-core` helpers for workflow line pricing, grouped row patches, HPT door summaries, shelf row patches, record hydration, and save payload generation.
- `@gnd/sales/sales-form-core` is treated as the native-safe sales-form boundary for Expo; it must not export from web UI TSX files or any module importing `@gnd/ui`, and this boundary is covered by `packages/sales/src/sales-form-core.native-safety.test.ts`.
- Expo app source must not import the web UI package directly; `apps/expo-app/src/features/sales/invoice-form/native-ui-boundary.test.ts` scans mobile source for `@gnd/ui` and direct `packages/ui` references so mobile remains native-owned while sharing only pure sales-domain helpers.
- Shared sales-form workflow helpers use one domain metadata parser for object-or-JSON metadata shapes, keeping mobile reopen behavior consistent without duplicating parser code across domain and workflow modules.
- Mobile save/edit/update flows remain routed through the invoice form store:
  - `actions.patchLineItem(...)`
  - `actions.buildSavePayload(...)`
  - `toSalesFormSaveDraftPayload(...)`
  - `newSalesForm.saveDraft` / `newSalesForm.saveFinal`
- Mobile save/finalize requests treat the persisted invoice/quote record as the user-facing completion boundary. Server-side document snapshot expiration, inventory sync queueing, and document warmup queueing run as bounded best-effort post-save tasks so slow document/cache infrastructure or running mobile without the jobs worker cannot leave the mobile form stuck on `Saving invoice...`; the mobile client also sends mutations over an unbatched tRPC link, bounds the save await path, and drives the overlay from store `saveStatus`, so hung requests surface a retryable footer error instead of permanently blocking the form. Successful mobile saves invalidate the sales dashboard, order list, quote list, sales overview, and sales filter query caches so newly created/updated documents appear in Recent Sales and list screens without manual refresh. Successful mobile create/update finalization presents a full-screen success modal with `Go to overview`, `Continue editing`, `Create new`, and `Go home` actions; overview/create-new/home actions dismiss the sales stack before replacing routes so users cannot swipe back to the success state. Persisted edit records hydrate onto the final review step by default.
- Dev-mode invoice save debugging now captures parsed `newSalesForm.saveDraft` / `saveFinal` payloads on the API server before the core save transaction. Captures are written under `debug/new-sales-form-save-payloads/YYYY-MM-DD/*.json` at the project root with summary metadata plus the full payload, are ignored by Git, run only when `NODE_ENV === "development"`, and never fail the save if the file write fails.
- The mobile invoice form store parses line metadata through the shared sales-form parser before matching existing source lines or propagating taxable state into grouped service rows, so JSON-string metadata does not break native line merging or grouped row updates after reopen.
- The shared sales form schemas preserve rich grouped payloads with pass-through fields for:
  - `formSteps`
  - `shelfItems`
  - `housePackageTool.doors`
  - grouped row metadata such as `meta.serviceRows` and `meta.mouldingRows`
- Mobile invoice summaries now expose `grandTotal` as the C.C.C-exclusive order
  principal, `ccc` separately, and `totalWithCcc` for credit-card/link/terminal
  display. The Costs, Review, and fixed footer totals use `totalWithCcc`, while
  save/accounting payloads retain `grandTotal`.
- Mobile ordinary, shelf, HPT, and grouped line calculations use the shared
  decimal-safe money helpers. Shelf and grouped line totals remain
  authoritative; two-decimal average rates are display-only and carry
  `rateRoundingAdjustment` metadata when they cannot recompose the row sum.

## Mobile Step Editors

Specialized invoice line editors now live under dedicated step-family folders:

```text
apps/expo-app/src/features/sales/invoice-form/steps/
  house-package-tool/
  moulding/
  service/
  shelf-items/
  shared/
```

`line-item-card.tsx` composes these editors instead of owning every grouped row UI inline.

## Behavior Notes

- Fresh mobile invoice/quote form state defaults `paymentMethod` to `Credit Card`, and a focused store regression test pins that default for newly reset mobile forms. Mobile create/bootstrap hydration also preserves that order default through shared save payload composition, so records with missing `form.paymentMethod` normalize to `Credit Card` before summary calculation and no longer save `paymentMethod: null` / `summary.ccc: 0` on first create.
- The mobile sales dashboard floating `+` action uses the reusable `FloatingFooterActionChooser` pattern built on the shared `FloatingBottomSheet` primitive to show a native Sales/Quote/Dispatch/Cancel chooser with backdrop tap, Gorhom pan-down dismissal, and list-row actions. Sales and Quote reset the in-memory create form, apply the selected type, and route to the typed customer-selector route before entering the invoice form; Dispatch opens the existing sales dispatch search route. Sales maps to `order` mode and Quote maps to `quote` mode through a focused helper with coverage, and the old inline `New Invoice` dashboard card is no longer shown. New, edit, and customer-selector invoice routes share one native route-param normalizer for the sales form type.
- Initial mobile invoice/quote creation no longer mounts the customer selector as a separate native stack route before replacing it with the invoice route. The invoice form renders `CustomerStep` inline until a customer is selected, then reveals the item workflow in the same screen tree, avoiding Android edge-to-edge native view removal during customer selection.
- The invoice item swipe/workflow wrapper uses stable React Native `style` for its fixed minimum height instead of the arbitrary NativeWind class `min-h-[640px]`, avoiding `react-native-css` variable-set remount warnings during the customer-to-items transition.
- Android sales-form helper routes for customer selection, sales details, and door-size entry use ordinary card presentation while keeping the headerless full-screen visual shape; iOS keeps native full-screen modal presentation. This avoids Android edge-to-edge `react-native-screens` modal traversal crashes when customer selection immediately replaces the selector route with the invoice route.
- The read-only mobile sales details helper route uses a sales-form-aligned native layout for invoice/quote details: a subtle header divider, rounded card sections, structured customer/detail rows, status badge, formatted dates, and a card-style totals section. The grand total is fixed in a soft bottom summary bar so it remains visible while details scroll. The route remains read-only and keeps the existing sales-details props and navigation behavior.
- Android edge-to-edge is disabled in `apps/expo-app/app.config.ts` after the crash continued inside `EdgeToEdgeReactViewGroup`; testing this mitigation requires installing a fresh Android native build, not only reloading Metro or applying an OTA update.
- The mobile sales dashboard uses a structured loading skeleton instead of a centered spinner, preserving the page's stat-card, action-row, and recent-sales-card rhythm while the overview query is pending.
- The mobile sales dashboard now exposes a live `Quotes` entry beside `Orders`. The quotes route reuses the mobile orders list architecture for back navigation, search, filters, pull-to-refresh, empty state, and document cards while querying `sales.quotes` and `filters.salesQuotes`; quote cards show quote-specific status (`Paid`, `Open`, `Part paid`) from invoice pending/total amounts, omit remaining-due/payment-progress list copy, and open the shared flat document overview by quote number. The quote form is still available from an explicit sticky `Edit Quote` action when the quote has a saved slug. Orders use the same overview shell with order-specific due/payment labels and sticky `Create Delivery`; the primary overview order follows the supplied flat ledger template with Summary, Financial, Customer Contact, Items, Activities, and Deliveries, omitting Shipping from the main scan path. Both document types share the `sales.getSaleOverview.overviewItems[]` fallback for line items, while orders prefer dispatch-enriched rows when available and preview the first five items before offering a `View more` affordance. Customer Contact and Items use full-card-width row dividers while keeping row content padded, and the top Total/Paid/Due amounts use stronger value typography. Overview screens show explicit unavailable/not-found states instead of rendering fallback `$0.00` customer data when the live overview query fails or returns no record. The overview scroll view supports swipe-down refresh, and the sticky order/quote action respects the device bottom inset so `Create Delivery` remains visible. The order/quote overview overflow rollout is tracked in `brain/plans/2026-06-28-mobile-sales-overview-more-actions-rollout.md`; Phase 0/1 adds a custom-pressable More button and floating sheet with a dev-only `Edit Order` / `Edit Quote` action, and Phase 2 adds a dev-only `Copy` tab that offers `As Order` and `As Quote`, uses smooth fade/slide tab transitions, reuses `sales.copySale`, invalidates dashboard/list/overview queries, and opens the copied document editor when a slug is returned. These staged actions stay hidden from preview and production until promoted. Mobile edit-mode sales forms now initialize inline workflow step pills on the last usable pill, while create-mode forms still start from the first pill.
- Mobile Orders, Quotes, and dispatch-search list search now opens from a floating footer pill backed by the shared `FloatingBottomSheet` primitive. Orders and Quotes draft search plus filters inside the sheet and commit only on Apply, Cancel preserves the current results, and dispatch-search uses the same sheet in search-only mode.
- The mobile sales dashboard Recent Sales section renders the shared `SalesInvoiceListCard2` flat ledger order card used by the Orders and Quotes lists instead of a bespoke compact row, mapping the dashboard recent-sales payload into the card's invoice total/paid/due, customer, phone, date, and delivery-option fields. The previous `sales-document-card` component has been renamed into `sales-invoice-list-card-1` for the old template, while `sales-invoice-list-card-2` is now the default mobile sales invoice/order list card; both templates share neutral invoice-list formatting helpers in `sales-invoice-list-card-utils`.
- Mobile sales-form selection rows now use the same tactile press feedback as the Orders, Quotes, and Recent Sales cards: the New Invoice trigger/type chooser, customer rows, item sheet rows/actions, shelf product search rows, and service suggestion rows use the app `Pressable` haptic/ripple path with `active:opacity-90` while preserving existing disabled states and layouts. The invoice form footer actions also trigger a light haptic on Back, Save Draft, Continue/Create/Save, and the item switcher icon so commit/navigation actions feel tactile.
- New invoice route params, form initialization, save labels, and the customer selector preserve the selected sales form type through the shared route-param helper.
- The dedicated mobile customer-selector route keeps customer results as a virtualized list while moving search into a fixed bottom input powered by `react-native-keyboard-controller` `KeyboardStickyView`, so the search control stays reachable and rises above the keyboard. The inline initial customer step inside the invoice form keeps its header search layout.
- Details edit/read-only, workflow notice, local-recovery, item-selector, and item-sheet surfaces use quote-aware labels for the screen title, details section heading, document reference number, line-item helper copy, sparse item fallback titles, recovery banner, quote date, good-until date, save/create actions, and notes/memo copy instead of leaking invoice-only wording.
- The Select items fullscreen picker keeps category/custom filters in the header while moving item search into the bottom `KeyboardStickyView` footer with the Clear/Add actions, so the search input and selection actions rise together above the keyboard and the result list reserves space behind the footer.
- The visible mobile local-recovery alert for unsaved local edits is hidden on the invoice form screen. Local recovery snapshot read/write behavior remains in place, but the screen no longer shows the amber "Unsaved local edits were found" banner.
- Customer/item selector helper text, item list actions, item sheet labels, and sparse item fallbacks use quote-aware wording when the selected sales form type is `quote`; the customer selector and customer list receive the route type directly so first render does not flash invoice-only copy or query the wrong recent-customer type. Customer selector, review, and details surfaces share sparse-customer display fallbacks so recent/search rows do not render dangling contact separators or blank billing copy when phone or billing address data is missing.
- Create/reset state starts with no mock/default selected customer; customer state is set only by explicit customer selection, selected-customer bootstrap hydration, recovered local payloads, or an existing saved record.
- Fresh create/reset/bootstrap state starts with no mock/default line item before customer selection, but explicit customer selection seeds the first blank workflow line when the form has no items so the original inline `Item Type` step pills and component grid appear immediately after the customer handoff. Empty create-mode bootstrap hydration preserves that selected customer's seeded workflow line, or reseeds it when the bootstrap already carries a selected customer, so the post-customer route handoff cannot clear the first item. The items screen groups the seeded first line as a workflow section keyed by line uid, keeping it active for the inline workflow selector and its step pills. The empty-state Add item action remains available for itemless states, while shared save validation rejects saving without a line item.
- Create-mode form bootstrap receives the selected customer id from the customer selector handoff, so server bootstrap data cannot overwrite the chosen customer with an unscoped default.
- The customer step defaults to 10 unique recent customers for the selected type, grouped by customer and ordered by each customer's latest order/quote timestamp; the mobile request shape is centralized in a focused helper so blank order/quote customer screens always request `recent: true`, the selected type, and `limit: 10`, while typed search text switches the list to explicit search results. The API overfetches recent customer groups before hydration so missing stale customer references do not under-fill the default list.
- Workflow component cards, workflow-root item selector rows, created/hydrated workflow line-card titles/subtitles, inline item description inputs, review item titles, workflow step selection labels/pills, Door Size picker headers, HPT door/group labels, HPT add/swap door option chips, custom component option labels, workflow selectable search rows, and newly added workflow-line descriptions no longer show UID text or UID-like workflow slugs as visible copy or title/SKU/value/category fallbacks; UID remains available for identity, keys, routing, search matching, and state. Workflow selector step pills now key off the shared selection-state helper rather than only `step.value`, so reopened JSON-selected components without a stored `value` still show the safe selected component label instead of falling back to a generic step title.
- Mobile workflow component mappers and the component grid use the shared UID-safe workflow selectable copy helper, so component cards fall back to a human `value` or neutral `Component` label instead of rendering UID-like `title` values. When a workflow step has more than 15 component results, the selector shows a native search input directly below the step pills and filters the current component grid in place without changing the active step or selection state. Inline workflow components remain part of the full invoice form scroll; when the step pills reach the top of the invoice content area, the step pills and optional search stick under the invoice header while the component cards continue scrolling. When component selection, proceed, or a step-pill action opens another inline workflow step, the invoice form smoothly scrolls back to the top of the workflow content so the next step does not inherit the previous component-list scroll position.
- Floating invoice actions use a shared screen-level host inside the invoice form shell, so workflow Proceed, Custom, and footer-safe actions align consistently above the form footer instead of scrolling with the item content. The host refreshes a registered action only when its explicit refresh key changes, keeping React node identity churn from repeatedly unregistering or replacing active actions. Normal footer actions slide down and fade when the user scrolls down through the invoice form, return on upward scroll, keep a stable fixed bottom scroll reserve while hiding/showing so content height does not jump, and keep centered workflow Proceed lanes visible by easing them lower when the footer actions are hidden.
- The inline workflow Proceed overlay is rendered with React Native style-only primitives, including explicit themed primary background and foreground colors, so it does not mix NativeWind `className` with custom `style` on the same element or lose its visible button background during mobile re-renders. Inline Proceed uses its own compact centered pill, visible-footer lane equal to footer action height plus 15px, and drops closer to the bottom edge when the main invoice footer hides, without changing the Custom action lanes.
- The invoice item switcher now lives as a footer clipboard-list item icon button to the right of the Create/Save action, while workflow floating lanes remain a pure layout contract for Proceed and Custom actions above the footer. The item sheet opener is registered upward from `ItemsStep`, keeping item-section state owned by the item workflow instead of coupling it to the footer. The form header also reflects the active item title (`Item x` when unnamed, or the entered item name/description when set) as a dropdown-style button with a chevron that opens the same item switcher sheet. Multi-item forms keep the left/right chevrons fixed at the middle of the screen instead of inside the scrolling item body, and also support horizontal swipes: swipe left advances to the next item, and swipe right returns to the previous item. Swiping now gives immediate visual feedback by sliding the active invoice item with the finger, resisting unavailable first/last item edges, springing back on short swipes, and using the same slide transition for side-chevron taps. The switcher sheet uses the same shared floating bottom sheet primitive as the New Invoice Sales/Quote selector, including detached floating presentation, backdrop close, pan-down dismissal, dynamic content sizing, and the flat divider-list row style for existing items, selected state, the New item action, and Cancel; item rows keep compact `Item 01` subtitles plus quantity/line-count/total summaries. The old inline top item-title input has been removed from the item workflow surface; item rows now expose edit/delete icon actions instead of a right chevron. Edit opens a compact `Edit Item x Title` view inside the same floating sheet instance, autofocuses the title field, uses Gorhom's bottom-sheet text input, and keeps the input plus Cancel/Proceed actions inside a sheet-owned `BottomSheetKeyboardAwareScrollView` so Android keyboard resize does not translate the controls out of the clipped floating sheet. Delete opens a confirmation view inside the same sheet instance and returns to the list when canceled.
- Custom-component capable workflow steps now expose a centered floating `Custom` action that opens a module-stable two-snap sheet at the fullscreen search index with `Custom Component Title` as the leading input label and an explicit full-width fixed-bottom `Proceed` button, explicitly snaps down to compressed `Title & Cost`, resets cleanly on dismiss, recognizes custom support from form-step metadata, route-step metadata, and selected-step custom snapshots, lists only non-archived custom options across object and JSON-string metadata shapes, normalizes custom titles and sparse fallback labels to uppercase like the website, treats typed exact-title matches like selecting the matching list result, clears the selected existing custom option when the compressed-title edit no longer matches, prefers stored custom pricing for the editable cost field, uses the website's null-vs-zero custom price-change semantics when deciding whether to upsert, selects an unchanged existing custom option without a backend update, saves/updates changed or new customs through `inventories.upsertDykeCustomStepComponent` with the same custom metadata/image marker used by the website, archives custom options through the website-equivalent `inventories.archiveDykeCustomStepComponent` path and refreshes the component source afterward, derives the selected sales price from the entered cost and active customer profile coefficient, selects through the shared workflow selection helper with the same forced-selection intent the website uses after custom save/select, and advances the workflow.
- The custom component details `Proceed` enablement and save handler match the save/select path: unchanged existing custom options can be selected without requiring a step id or backend upsert, while new or changed custom components still require a valid step id before saving, and the mutation handler returns early if the details state is not saveable.
- Mobile custom-component, line-workflow, shelf subtitle, HPT grouping, default profile, and seeded shelf-row helpers all use the shared sales-form metadata parser rather than local JSON parsing, keeping custom/HPT/shelf behavior on the same object-or-JSON metadata contract as shared core.
- Shared workflow component edit actions parse selected component and step metadata from object or JSON-string shapes, so mobile component price/image/redirect/section override edits preserve reopened selected-component snapshots without spreading string metadata.
- Shared workflow selection snapshots now parse component `_metaData` from either object or JSON-string shapes before checking the custom marker, so mobile custom components remain marked as custom through single-select, multi-select, Select All, routing, and visible-component filtering instead of leaking into normal component lists or losing selected-custom metadata.
- Shared workflow selection actions parse selected component, redirect, and select-all step metadata from object or JSON-string shapes, so mobile component selection/proceed/redirect updates keep using the website-equivalent routing path after edit/reopen.
- Shared workflow record helpers parse selected UID/component, selection-label, and component override metadata from object or JSON-string step metadata, keeping mobile selected-card state and edit override hydration aligned with reopened records.
- Shared workflow step selection maps parse `selectedProdUids`, redirect-disabled flags, and component price dependency metadata from object or JSON-string shapes, so mobile reopened lines keep selected custom visibility, multi-select rule evaluation, redirect-disabled navigation, and dependency pricing aligned with the website flow after hydration.
- Shared workflow calculators parse shelf row pricing/category metadata, door-size variation metadata, and HPT door summary metadata from object or JSON-string shapes, keeping mobile shelf, door-size, and HPT summary calculations aligned with reopened new-sales-form records.
- Mobile-native display and editor surfaces parse line, row, and patch metadata through the shared sales-form parser before grouping invoice items, hiding UID subtitles, merging door-size patches, and showing HPT base-cost/price-missing state, so reopened JSON-string metadata remains aligned with the website-equivalent core logic without importing the web UI package.
- Shared route rebuild helpers parse configured-step, existing-step, item-type selection, and line metadata from object or JSON-string shapes, so mobile edit/reopen flows preserve selected root components, configured route metadata, redirect-disabled state, and custom next-step fallback routing when the saved form carries stringified metadata.
- Selected custom components are merged from active-step snapshots when needed and pinned first in the component grid using the same object/JSON-string metadata handling as custom option detection, including snapshots stored on the active form-step metadata and/or nested route-step metadata; unselected custom components remain hidden from the normal component list. Empty outer selected arrays no longer mask nested route-step selected custom snapshots or selected UID ordering.
- Door-size selection requires at least one selected quantity before advancing, and the route defers navigation until after the workflow selection callback has run. The mobile `Next step` path now applies selected door-size rows and then explicitly runs the multi-select proceed action so the underlying workflow moves past Door instead of staying on the current multi-select step. The same fullscreen picker can be reused by HPT line-item size editing, but that path labels its primary action `Apply` and hides the secondary `OK` action because it updates the current HPT section rather than advancing workflow steps.
- Mobile Door/HPT route flags now use one native helper that matches the website HPT panel semantics: `noHandle` and `hasSwing` are true only when the route config explicitly sets boolean `true`, so missing or stringy `hasSwing`/`noHandle` values do not render or persist swing/handle values in mobile HPT/door-size patches. The HPT row editor also treats omitted `hasSwing` as false, keeping the Swing field hidden unless the route explicitly enables it.
- Door-size shared utilities parse row and step metadata from object or JSON-string shapes for missing-price state, configured base price, selected component identity, supplier metadata, stored-row normalization, and supplier repricing, so mobile edit/reopen behavior stays on the shared new-sales-form door pricing path.
- Door/HPT shared action helpers parse step metadata from object or JSON-string shapes when changing suppliers, swapping selected doors, adding HPT door options, removing selected components, and summarizing selected doors, preserving selected-component snapshots without spreading string metadata.
- Shared selected Door/Moulding selector helpers use the sales-form metadata parser for outer/nested step metadata, fallback component fields, and persisted moulding rows, so mobile edit/reopen restores selected snapshots consistently from object or JSON-string metadata.
- Door and moulding multi-select steps show a centered pill-sized `Proceed` action once at least one component is selected; the action advances the workflow while staying above the bottom footer controls without stretching full width. Overlay workflow selectors register directly with the screen-level floating host, while inline workflow selectors publish a keyed Proceed callback plus footer offset through `ItemsStep` and `InvoiceFormScreen` so the screen owns and renders the actual absolute overlay button above the invoice footer. Inline Proceed stays fixed near the screen bottom instead of appearing at the end of the component list, and it eases farther down when the normal invoice footer hides and back up when it returns. Selected-count detection reads both outer form-step metadata and nested `step.meta` metadata, including JSON-string shapes, and can also fall back to visible selected component state, selected component snapshots, selected moulding count, selected `meta.mouldingRows`, and local pending Door/Moulding card taps while line metadata rehydrates, so new and reopened Door/Moulding records still show the Proceed action when at least one component is selected. Proceed visibility is now hard-gated to active multi-component picker steps only; selected metadata, line-item editors, HPT, shelf/service final steps, item type, and regular single-select component steps cannot surface the floating Proceed button. Door/Moulding selected state remains an explicit contract inside that component-picker gate, and moulding item category pills such as Flat Board are classified as moulding selection steps until the final Line Item editor. Door card taps persist the selected component before opening Door Size so multi-door selections have countable step metadata immediately. A native pure visibility helper and regression test pin this behavior for Door, Moulding, generic bulk-selectable multi-select steps, selected-row fallback, selected-snapshot fallback, pending-tap fallback, category-step fallback, non-picker hidden states, single-select hidden states, and zero-selection states.
- House Package Tool is a first-class mobile section with package totals, active-door chips, add-door navigation, supplier-sensitive active-door size configuration, active-door swap/removal, and stacked size rows. The active inline workflow `House Package Tool` step renders the functional mobile HPT editor directly instead of the old notice-only placeholder, while preserving the existing step-pill flow. Add door returns the inline workflow to the Door multi-select step so additional doors are selected through the normal Door grid/proceed path instead of from a separate HPT candidate card; configured size rows and totals remain unchanged until quantities are chosen. Reopened door/HPT lines restore selected door snapshots from either outer form-step metadata, nested route-step metadata, or persisted HPT rows with available component candidates, including JSON-string metadata shapes. Rows now match website pricing semantics: configured size is derived with component uid/title metadata, LH/RH quantities derive total unless the route is no-handle, missing supplier/size pricing blocks quantity entry, base-cost edits recalculate sales pricing while preserving surcharge, add-on and nullable custom overrides remain editable, and an expandable estimate breakdown shows priced workflow steps, door/base/surcharge/calculated/final pricing, quantity, and line total. The invoice form shell uses a keyboard-aware main scroller so focused inline HPT size-card inputs can scroll above the keyboard without a competing `KeyboardAvoidingView`. Setting or clearing a custom override recalculates the row final unit and line total immediately while preserving/clearing legacy override metadata, so the row truly returns to automatic pricing when custom price is blank. Hydrated HPT size rows attach back to the selected door group by numeric product id or stored component uid metadata from object or JSON-string row metadata, and component-less legacy/imported size rows are shown under the only selected door like the website panel fallback instead of splitting into an empty selected group plus a manual group. Group display labels plus stored quick-size and created-size component titles prefer human component value/title copy over UID-like workflow slugs, so imported/custom selected doors do not split into a manual group or leak identifiers as names. Active-door removal preserves the current workflow route config when recalculating remaining door rows, including no-handle and swing behavior, and the mobile editor disables active-door removal when only one door remains selected so HPT follows the same at-least-one-selected rule as Door/Moulding multi-select.
- HPT route and door-selection helpers parse persisted form-step and line metadata from object or JSON-string shapes before applying selected-door snapshots, stored route config, and section overrides, keeping reopened mobile HPT lines aligned with the website route-config precedence.
- HPT legacy compatibility and normalization helpers parse step, door, and row metadata from object or JSON-string shapes for flat rates, door sales-unit pricing, surcharge/add-on/custom override fields, hydration, and totals so reopened mobile rows follow the same shared new-sales-form pricing path.
- Shared record normalization and legacy grouping helpers parse route-config and grouped-row metadata from object or JSON-string shapes, so mobile create/edit hydration, grouped service/moulding expansion, and no-handle HPT normalization keep the canonical save payload aligned with the website logic.
- Customer-profile repricing uses the shared metadata parser for workflow selected components, shelf rows, HPT door rows, and stored door route config, so changing or bootstrapping a mobile customer's profile keeps reopened JSON metadata on the website-equivalent repricing path.
- Sales-form summary costing reads service taxability, service/moulding labor metadata, and shelf row price fallbacks from object or JSON-string metadata. Credit-card convenience charges are derived into returned/hydrated display grand totals and print/payable totals from the selected payment method plus `ccc_percentage`; order persistence keeps base sales totals and amount due free of the derived charge.
- Mobile sales form details, sales overview, Orders/Quotes lists, and Recent Sales cards use the API's display-only C.C.C fields for visible card-adjusted totals/pending amounts while preserving principal `total`, `paid`, and `pending` for payment progress and saved order accounting. The read-only form totals panel now shows `C.C.C` whenever the selected payment method derives a convenience charge, matching the Costs and Review steps. The mobile sales overview Financial card now also mirrors the website overview finance breakdown by rendering the selected payment method, principal order progress stats, conditional `Card Paid` / `Card Pending` C.C.C stats from server `costLines`, and the full invoice cost-line ledger including rows such as `C.C.C`, `Charged to Card`, and `Total Due With C.C.C`.
- Service rows support add/remove plus service text, qty stepper/direct entry, unit price, tax, and production toggles. Stored service rows are read from object or JSON-string line metadata in the mobile edit context and shared record hydration path, and save patches merge parsed metadata rather than spreading string characters. The inline final Service `Line Item` step now renders the native service row editor below the workflow pills instead of a placeholder notice; user edits patch through shared service-row helpers, while inline mount sync stays disabled to avoid invoice-line update loops. Mobile service-row edits preserve the parent invoice item description/title instead of replacing it with the first service row or joined service-row labels, because a single invoice item can contain multiple service line items. Service text now normalizes to uppercase while preserving the active typed value, including a trailing space, so the native controlled input does not eat the space bar while the user types multi-word service names. Mobile service Unit/Tax/Prod editing now follows the website line-pricing capability boundary by allowing only Super Admin role titles to edit pricing/tax/production controls; other roles can still edit service text and quantity while seeing Unit as read-only. Service row deletion now opens a floating confirmation sheet before removing a row. The service editor's content now follows the provided mobile card design: a large line-total summary strip, rounded service configuration cards, an expandable multiline underlined all-caps Service input with a fixed right-side search icon, side-by-side Quantity and Unit Price controls, and a right-aligned row subtotal, while leaving the existing header, footer actions, and add-service placement unchanged. The Quantity editor is a framed mobile stepper with rounded minus/plus touch targets and a centered numeric input, aligned with the Unit Price field for a cleaner card row. The Service search icon opens a fullscreen native suggestion modal matching the Add shelf picker pattern; blank search shows recent unique DB-backed service names with latest observed price, typing filters suggestions, the search input auto-focuses and opens the keyboard after the modal appears, and selecting a suggestion fills the service text plus price when the current role can edit pricing. The Service total/quantity summary also participates in the invoice screen's existing sticky workflow header lane while the final Service editor scrolls, using a compact version of the same summary treatment. When the Service final editor first opens, it requests the existing invoice scroll container to reset just above the service editor so users see the normal inline summary first instead of landing in a pre-scrolled sticky state. The inline `+ Add service line` action now registers through the same centered inline floating footer lane as workflow `Proceed` and `+ Add shelf`, so it stays fixed above the invoice footer instead of rendering inline in the service section. That hidden add-service footer registration is guarded against handler identity churn and uses a stable active-line patch callback, preventing parent/child update loops when the final Service step opens. Empty/zero service Unit values render as an empty input with a `0` placeholder so users can clear the field on mobile without immediately seeing a sticky zero return.
- Moulding grouped line editors continue to use a flat divider-row design with plain summary strips, while Service uses the rounded card service-configuration layout. Inline grouped line-item steps skip the empty component-picker label/body, and the Service/Moulding editors do not add a duplicate top section label or divider above their totals. Service row item totals are visually emphasized with an extra-large, heavy amount treatment and supporting quantity/unit controls inside the service card.
- Moulding selection behaves as true multi-select in the component grid with stable-height cards; selected cards show quantity controls as an image overlay only while checked, so unchecked cards do not reserve extra card-footer space and selecting the first moulding does not change grid item height. Moulding grid images memoize their source objects and disable Android image fade so selection re-renders do not visually reload adjacent column images, and the inline Moulding step keeps a stable bottom Proceed reserve for the whole step instead of toggling layout height when selections appear. The Moulding component grid caps visible results to the workflow component max of 15 while search still filters the full component set before applying the visible cap. The inline final Moulding `Line Item` step now follows the website `StepSection` split: the workflow selector owns pills/component selection, while the active invoice item renders the real line-item-owned moulding row editor instead of the old placeholder notice. The moulding line-item section is a row-first editor with prominent actual moulding thumbnails, display-only moulding titles, plus/minus quantity controls with direct numeric entry, estimate and row total. Tapping a moulding thumbnail opens a fullscreen native image preview using the same resolved image URI as the row thumbnail. The calculator is opened from an icon-only row action and preserves the website-equivalent piece-length/total-LF/waste calculation panel; Apply updates quantity through the canonical grouped-row patch. Add-on and nullable custom price are configured from a per-row More bottom sheet, and the main list only shows Add-on/Custom chips when those values are configured. Like the website, the final selected moulding row cannot be removed from the line editor or through the shared moulding removal helper, and direct row edits persist at least quantity 1 for every selected moulding row. Existing grouped row identity fields are preserved when deriving edit rows. Reopened moulding lines restore selected component snapshots from either outer form-step metadata, nested route-step metadata, or persisted `meta.mouldingRows`, including JSON-string metadata shapes; stored moulding rows are also read from JSON-string line metadata in the mobile edit context and shared record hydration path, and save patches merge parsed metadata rather than spreading string characters. Moulding save/remove action helpers use the shared metadata parser for object or JSON-string line/step metadata when reconciling selected UID/component snapshots and writing grouped rows.
- Shelf items use a simplified mobile picker and line-item-owned final step: the inline final `Shelf Items` workflow step renders the real native shelf editor instead of the old placeholder notice, while the workflow selector continues to own pills and component routing. Selected shelf rows use a simple flat layout with title, quantity stepper, category-tree subtitle, editable unit price, prominent total, and remove action. The `+ Add shelf` action registers through the same centered inline floating footer lane as workflow `Proceed`, so it stays fixed above the invoice footer instead of rendering inline in the shelf section; the empty `No shelf items selected` message is centered horizontally and vertically inside a responsive screen-height empty-state area. Entering the final Shelf Items step from the workflow auto-opens the `+ Add shelf` picker once for that active shelf editor, so tapping a shelf component immediately lands in product search. `+ Add shelf` opens a fullscreen search modal using the same app safe-area wrapper as the customer selector so the header does not collide with the Android status bar, and the search input auto-focuses and requests the keyboard when the picker opens. The picker list stays virtualized, and search is a sticky bottom footer powered by the keyboard-controller sticky view so the input rises above the keyboard instead of being covered. Blank search requests up to 15 visible shelf products, ordered by recent shelf usage first and then active product fallback when usage history under-fills the list; typing searches shelf products directly, and selecting a result returns to the shelf step with the item listed and category-tree subtitle. The mobile picker does not merge already-selected shelf product detail rows into its result set, so blank search remains a default/recent list instead of being padded by selected items; this request shape is centralized in a focused helper with coverage. Archived/hidden recent products are skipped without under-filling because the API scans recent shelf usage in batches until older visible recents fill the requested limit or usage is exhausted, then fills remaining blank-search slots from active shelf products. Search results use a flat selector-list style with loading skeleton rows and include product title, category tree, unit price, and one ghost chevron action; the chevron opens a compact invoice-item-style bottom sheet with Edit, Delete, and Cancel actions. Edit opens an inline name/price form and save returns to the search list, while Delete opens an inline confirmation before soft-deleting the shelf product and clearing matching selected rows. The inline shelf editor disables mount-time sync so entering the final Shelf step does not call `patchLineItem` until the user selects a product, changes quantity/price, removes a row, edits a product, or deletes a product. Selected shelf-row category subtitles and shared shelf row price/product patch helpers read and merge metadata from both object and JSON-string row metadata so hydrated rows keep their category tree and price overrides without spreading string characters. The old mobile category-bucketing picker helpers were removed so this feature stays intentionally simple; the UI remains mobile-native while still writing canonical `shelfItems` through shared shelf section patch helpers.
- After saving a shelf product edit in the fullscreen picker, the mobile search query resets before returning to the list so the updated product is not hidden by the stale pre-edit filter.
- HPT, service, moulding, and shelf changes continue to write through the shared core patch helpers so create, edit, and update saves preserve canonical payload shape.
- Mobile invoice-form imports from `@gnd/sales/sales-form-core` are covered by a barrel export smoke test for mobile-critical workflow helpers, including shelf category traversal and HPT door swap helpers.

## Validation Guidance

- Manual mobile QA should cover create and edit saves for mixed invoice lines containing HPT doors, service rows, moulding rows, and shelf items.
- Lightweight static validation is preferred by default in this Bun monorepo; broad Expo typecheck/build/browser validation should be run only when explicitly requested.

## Known Gaps

- 2026-06-23: Mobile invoice creation can still hit the 30 second client save timeout even after unbatched mutation transport and bounded post-save tasks. The next tracked closure plan is `brain/plans/2026-06-23-bug-fix-mobile-invoice-web-parity-and-save-reliability-gap-closure.md`, which treats request/stage observability as the first blocker and then closes web/mobile form parity gaps with golden payload fixtures.
