# Production material availability

## Current implementation (2026-09-09)
The top of Production shows missing inbound or remaining material availability for scoped tracked needs. Unfinished inbound information takes priority. Historical completed receipts do not suppress a remainder. Unknown setup, received-but-unapplied stock, and blocked review evidence retain truthful attention. Submission completion alone does not prove material coverage.

Admins can open Inventory/Needs or mark materials available; workers use the existing receiving policy and assignment scope. The main button opens the shared checkbox/quantity form. Its companion menu offers Mark all as available → Suppliers (N/A first) → Received date. A single click selects a date; double-click or Save availability commits. Future business dates are rejected. Success closes the active form/calendar, keeps the originating tab and refreshes material, production and sales queries.

The scoped command revalidates permissions, lifecycle, quantities, supplier and reviewed revision in a serializable transaction. It creates and receives the selected inbound, applies only that receipt's new demands, reconciles eligible submission reviews and refreshes canonical projections. A durable actor/request identity prevents duplicate receipts. Per-component capacities survive merged UI rows. Receiving and allocating the same quantity are not additive coverage.

Receipt allocation evidence records quantities by component and stock so later allocation splits or note edits do not cause duplicate receipts. Established receipt allocation notes support older records; genuinely unknown historical provenance stays conservative and may require Inventory review. Existing unrelated review/cancellation work is preserved; this command adds no blanket approval or cancellation authority.

## Validation
- 199 focused domain/API/UI-helper/query-event tests pass (550 assertions).
- 13 isolated local database tests pass (73 assertions): partial4/2/4, all, replay, concurrent requests, rollback, supplier/stale scope, worker policy/removal, merged capacities, unrelated stock, note changes and allocation splits.
- Browser: 09602PC displays four needs/58 units without modifying the order. A disposable order saved4 through the form, retained6 pending, then saved6 through the BHI supplier/date double-click flow; Production remained open and the material banner disappeared. Two durable writes and the selected received date were verified. Stale form refresh preserved quantity4. Desktop verification completed; broader responsive and worker browser coverage remain in the task.
- Typechecking is not globally green: Sales has the preexisting copy-sales.ts:521 nullability error; dashboard has existing test matcher/form type errors; root typecheck stops in Settings on NodeNext import extensions. No diagnostics in the new availability files from the completed scoped checks.

## Extension in progress
Completed-shipment synchronization is implemented: unapplied demand quantities are derived from good receipt quantities after subtracting quantities already applied to any linked order. Only the actor's active component scope is updated, then canonical reservation and eligible submission finalization run in the same transaction. No new stock receipt occurs. Combined validation passes 18 local transaction tests / 152 assertions and 58 focused regression tests / 193 assertions. Remaining work is browser/role verification, review and scoped commit, not another material-review step.

The user-facing action is now **Sync**, under **Received materials need to be synced to assignments**. Production and calendar show one synchronization panel instead of a “received materials or allocations need review” banner followed by a separate action. Clicking synchronizes directly without a review screen or second approval step. It applies eligible inventory coverage and finalizes eligible submitted work in one transaction, then refreshes Production. Any inconsistent quantities remain pending with concise information; there is no extra review step for valid material coverage. Verified the synchronization panel on 09439PC without submitting it; the final heading was subsequently clarified.

Received-but-unallocated component quantities can now be reserved to scoped needs without a new receipt. Shared stock budgets and durable receipt/allocation evidence prevent over-allocation and historical receipt reuse. The public summary exposes eligible received units, not internal stock plans. Three local database cases pass / 54 assertions. Completed shipments whose demand receipt quantities were never applied still require the remaining shipment-to-Needs application work.

Apply covered materials now also applies valid existing allocation proposals before rechecking pending reviews. It preserves over-allocated or stock-inconsistent components and reports residual attention. The calendar preview for 09439PC identifies two applicable allocations while retaining its inconsistent components for Inventory review; this was inspected without saving. Two local database cases pass / 36 assertions. Received material without a valid allocation proposal still requires the remaining Needs-application work.

The read-only covered-review summary and transactional reconciliation API are implemented for already-covered eligible pending reviews. The command reuses established review/payroll rules, protects retries with an actor-bound audit identity, and refreshes canonical production projections. One local database test passes with 16 assertions; API/query-event checks pass 44 tests with 164 assertions. No new receipt or stock movement is created.

Production now composes the existing panels with an explicit Apply covered materials control for eligible reviews. Calendar schedule cards expose View material actions in a persistent popover and load the same controls only on expansion. The existing selection form renders inline with unique form IDs. Inventory links target the expanded order. Desktop read-only browser checks confirm form opening/cancel, supplier/date nesting, Escape behavior and navigation for 09602PC; broader responsive/worker and disposable save checks remain open. Safe application of received-but-unapplied Needs also remains incomplete. See [plan](../plans/2026-09-09-feature-production-missing-inbound-and-quick-availability.md) and [task](../tasks/2026-09-09-production-missing-inbound-and-quick-availability.md).

The standalone calendar material modal uses a bounded flex layout: its form body scrolls while the Cancel / Mark as available footer stays visible. The shared inline presentation also keeps actions separate from scrolling fields.

Calendar browser saves were verified on a disposable local order: partial4 then quick supplier/date6, followed by direct Sync of an eligible historical pending submission. The form/picker closed, the calendar stayed open, and resolved actions disappeared. Database checks proved two receipts, one synchronization and one payroll; the fixture was removed. Active-query event transport tests pass for both mutations. Fresh-page summary refresh and remaining role verification are tracked in the task.
