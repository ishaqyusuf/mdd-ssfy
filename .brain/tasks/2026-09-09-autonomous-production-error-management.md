# Task: Autonomous production error management

## Status
In Progress

## Priority
Medium

## Created Date
2026-09-09

## Last Updated
2026-09-09

## Global Ticket
- Ticket Position: 1/1

## Source Context
[Research and phased proposal](../research/2026-09-03-autonomous-production-error-management.md).
The user invoked implement-with-progress after the Midday research revision.
Implement the described reliability workflow on the current checkout/branch,
preserving the staged publication, remediation, and production gates. The research
has phases but no explicit ticket boundaries, so this is one canonical task.
Optional product candidates are evaluations, not mandatory new dependencies.

## Implementation Progress
- Completion: 10%
- Current Checklist: 6/20 — Add verified Vercel event ingestion and bounded log discovery
- Blockers: Deployable migration artifact remains unresolved: local schema/history drift makes Prisma request a full reset, which was not performed. Other local implementation can proceed. External publication destinations and production activation remain pending configuration/authorization gates.

## Implementation Checklist
- [x] Revalidate research, current branch, existing adapters, Brain contracts, and test boundaries.
- [x] Define and test incident intake and correlation contracts with scoped identities, impact severity, and bounded safe evidence.
- [ ] Implement durable incident/occurrence/delivery/transition persistence and local migrations.
- [ ] Add verified Sentry ingestion and bounded reconciliation.
- [ ] Add Trigger discovery, unfinished-run watch set, and terminal reconciliation.
- [ ] Add verified Vercel event ingestion and bounded log discovery.
- [ ] Add service ownership, incident action authorization, revision checks, and audit trail.
- [ ] Implement evidence packets, Brain/existing-work links, analysis confidence, budgets, and deterministic urgent fallback.
- [ ] Implement durable outbox claims, retry policy, uncertain delivery reconciliation, and integration health.
- [ ] Extend GitHub transport for incident publication/update and repository-scoped credentials.
- [ ] Implement Slack thread delivery, digests, acknowledgement/assignment/snooze actions, and actor mapping.
- [ ] Implement observation/recurrence/resolution contracts and independent stale-ingestion escalation.
- [ ] Restore stack-based error grouping and establish affected PR validation/release correlation checks.
- [ ] Implement the 20-case shadow corpus and verify complete local end-to-end intake through ticket/Slack delivery contracts.
- [ ] Prepare reviewable external connection configuration and perform authorized hosted ingestion/publication/owner checks.
- [ ] Evaluate analysis quality and record the shadow-to-publication gate with evidence.
- [ ] Deliver the separately enabled regression-test/draft-PR phase after its prerequisites or record its explicit owner disposition.
- [ ] Deliver the separately enabled guarded-recovery phase after its prerequisites or record its explicit owner disposition.
- [ ] Complete relevant final tests/typechecks, code review, and Brain impact updates.
- [ ] Commit only this task's work on the current branch and verify the full completion audit.

## Validation Evidence
- Added production-only opt-in Vercel polling registry with explicit runtime paths, separate read-token references, duplicate source rejection, batch/time budgets, and sanitized summaries. Two tests / 6 assertions and scoped Biome pass. Scheduler/temp-config provisioning remains pending. Prior turn was verified durable-checkpoint progress.
- Connected Vercel subdivision to durable cursor leases, awaited occurrence writes, page receipts, and cooldown on incomplete work. Local DB acceptance proves query-budget interruption resumes both saved half-windows and only then advances the original watermark (5 assertions). Scoped Biome passes. Runtime registration/scheduling and process-to-database acceptance remain pending. Prior turn was progress.
- Added bounded resumable Vercel window subdivision: saturated queries split into adjacent overlapping-boundary windows, completed windows leave the pending suffix, and unsplittable millisecond density fails explicitly rather than advancing. Checkpoints reject invalid/out-of-scope/noncontiguous windows and incomplete tails. Two tests / 6 assertions pass; durable cursor orchestration still pending. Prior turn was verified process-binding progress.
- Bound Vercel query preparation/process execution/normalization with explicit Node/CLI/config paths, VERCEL_TOKEN environment authentication, 5-second timeout and 2 MiB output cap. Installed CLI index.js confirms VERCEL_TOKEN support. Two boundary tests / 9 assertions and scoped Biome pass. Runtime packaging/config directory provisioning and durable discovery orchestration remain pending; no provider reads executed. Prior turn was progress.
- Connected bounded CLI decoding to scoped intake with strict timestamp/window validation. Saturation counts all rows before actionable filtering, preventing a mostly-informational full result from appearing complete. Two tests / 5 assertions and scoped Biome pass. Credential binding and durable subdivision/checkpoint execution remain pending. Prior turn was progress.
- Added bounded process execution for CLI fallback: absolute executable, argument array without shell, explicit environment, timeout, output cap, and sanitized errors. Two real local-process tests / 4 assertions verify literal arguments, nonzero exit, oversized output, and timeout. Scoped Biome passes. Vercel credential binding and query orchestration remain pending; no provider query executed. Prior turn was progress.
- Inspected installed Vercel CLI commands-bulk.js request-log mapping: JSONL uses request IDs, responseStatusCode, nested logs, and source aliases, not drain-native records. Added separate request-summary normalizer detecting nested error/fatal and 5xx without copying raw messages. Two tests / 7 assertions pass; scoped Biome passes. CLI defaults missing environment/timestamps, so executor must preserve explicit production/window constraints and cannot claim raw-provider completeness. Prior turn was progress.
- Revalidated installed vercel logs --help and current CLI docs. Added explicit production/team/project/no-branch query arguments plus bounded JSONL decoding with saturation detection (2 tests / 10 assertions; scoped Biome pass). Read all levels to avoid intersecting filters excluding 5xx/crashes. CLI records require separate normalization; executor, subdivision, cursor persistence and scheduling remain pending. No provider logs queried. Prior turn was progress.
- Signed deployment HTTP-to-database acceptance passes: different delivery IDs and repeated delivery produce one occurrence, one incident, two destination intents, and preserved deployment evidence (7 assertions). Prior turn was verified registration/mounting progress. Other lifecycle events, deployment correlation, Vercel discovery and hosted gates remain open.
- Mounted deployment-failure ingestion with separate RELIABILITY_VERCEL_DEPLOYMENTS registrations and RELIABILITY_VERCEL_WEBHOOK_SECRET_* references. Four HTTP/config tests / 13 assertions and scoped registration Biome pass. Endpoint/runbook documented. Other lifecycle events and hosted acceptance remain pending. Prior turn was progress.
- Added deployment-failure HTTP persistence handler: bounded raw body, required signature, scoped normalization, and awaited write before acknowledgement. Authenticated nonproduction failures are ignored; storage errors return sanitized 503. Two HTTP tests / 7 assertions and scoped Biome pass. Route/registration mounting remains pending. Prior turn was progress.
- Added bounded raw-body HMAC-SHA1 verification before deployment-event parsing, sharing signature verification with the drain adapter. Five deployment/drain tests / 20 assertions and scoped Biome pass. Primary signature contract rechecked at Vercel request-header docs. Deployment HTTP mounting/persistence remains pending. Prior turn was progress.
- Added current deployment.error normalizer after checking Vercel webhook reference. It validates team/project/production target and keys the occurrence by deployment+failure type, separately returning delivery ID. Safe deployment evidence retained; metadata excluded. Two tests / 9 assertions and scoped Biome pass. Signed deployment transport and other lifecycle event persistence remain pending. Prior turn was progress.
- Evidence contract verification: malformed/oversized identifier values reject, arbitrary evidence fields are excluded, and correlation metadata does not change occurrence identity. Fixed Vercel dotted trace.id support and conflicting-alias rejection after a red test. Full observability reliability suite: 28 tests / 118 assertions pass. Observability/DB typechecks pass for evidence persistence; scoped Biome passes. Prior turn was progress.
- Intake now reconstructs optional deploymentId/requestId/traceId/release identifier evidence with strict length/character validation, and persistence stores it in the existing occurrence JSON column. Vercel supplies deployment/request/trace IDs without messages or paths. Ten intake/Vercel tests / 43 assertions and local batch-replay acceptance / 13 assertions pass; scoped Biome passes. Prior turn was progress. Correlation policy still remains to be implemented.
- Verified current Vercel /v1/drains/test uses sample-event delivery validation; no unsigned-handshake bypass added from legacy assumptions. Documented compression none and implemented explicit 415 for compressed bodies (red/green test). Vercel HTTP suite: 3 tests / 9 assertions pass; scoped Biome passes. Hosted sample compatibility remains pending. Prior turn was progress.
- Verified Vercel HTTP-to-local-database recovery: first occurrence commits, second write fails and returns 503, then repeated whole-batch retries complete with exactly two occurrences and one pair of outbound intents per incident. Raw messages remain absent. Focused acceptance passes with 11 assertions. Prior turn was verified registration/route progress. Provider setup handshake, deployment lifecycle, log discovery, and correlation remain open.
- Mounted Vercel drain route with validated registrations and separate signing-secret references. Four HTTP/config tests / 13 assertions pass; scoped registration Biome passes. Added concrete configuration runbook, including pending provider handshake/batch compatibility and hosted acceptance. Prior turn was progress. Database batch replay remains next; nothing activated.
- Added Vercel drain HTTP handler with streamed 1 MiB limit, exact configured format, signature verification, whole-batch normalization before any writes, and awaited persistence before acknowledgement. Storage failure returns 503 so partial batches can replay through occurrence deduplication. Two HTTP tests / 6 assertions and scoped Biome pass. Registration/route mounting and real-database redelivery acceptance remain pending. Prior turn was progress.
- Added Vercel production log normalization using verified drain schema: error/fatal, 5xx including proxy status, and top-level lambda -1 crashes become unknown-impact candidates. Proxy -1 is not treated as a crash. Project/environment validation and raw-content exclusion tested (2 tests / 21 assertions). Native log IDs retain independent candidate identity pending cross-log correlation. Prior turn was progress; durable route/persistence and deployment lifecycle remain pending.
- Began Vercel drain transport using current primary docs: raw-body HMAC-SHA1 and JSON/NDJSON batches. Added bounded signature/batch decoder; 2 tests / 8 assertions and scoped Biome pass. Decoder records are untrusted until source/environment normalization; no Vercel route is mounted. Prior turn was verified progress. Trigger hosted/historical-health checks remain open while Vercel implementation proceeds.
- Historical Trigger database acceptance passes: repeated completed sweeps revisit the seven-day window, old failures retain one occurrence, and incremental polling uses a different cursor. Focused test: 9 assertions. Prior turn was progress. Historical freshness inclusion and authorized hosted checks remain pending; local implementation can proceed to Vercel without marking Trigger hosted acceptance complete.
- Added hourly Trigger historical discovery with an independent resumable cursor and seven-day replay window. Historical execution leaves watched-run retrieval to incremental polling. Scheduler remains behind the existing opt-in/production guard. Five scheduler tests / 21 assertions pass; full historical DB acceptance remains pending. Corrected the new scheduler test to assert the returned summary so internally caught callback assertions cannot yield false success. Prior turn was progress.
- Mounted authenticated GET /api/reliability/health and connected validated Trigger monitor registrations to the durable freshness query. Five HTTP/configuration tests / 20 assertions pass. Supports Trigger only; Sentry/Vercel health coverage and external uptime-monitor activation remain pending. Prior turn was progress. Runbook documents exact configuration and non-200/timeout expectations.
- Added authenticated aggregate HTTP health handler for an independent monitor. Missing/wrong credentials never invoke storage, empty/unhealthy source sets return 503, storage errors fail closed without diagnostics, and responses disable caching. Three boundary tests / 12 assertions and scoped Biome pass. Handler is not yet mounted or connected to configured source queries; independent monitoring remains incomplete. Prior turn was verified progress.
- Added a read-only Trigger freshness query for independent monitor integration. It distinguishes never-completed discovery, stale polling, old discovery watermarks, future cursor clocks, and stale unfinished watches. Retry postponement does not refresh watch health. Focused real-DB acceptance passes (1 test / 5 assertions). This is a query boundary only: independently scheduled transport/escalation remains incomplete. Prior turn classified as progress.
- Fixed Trigger watch/discovery fairness after a red integration test reproduced discovery starvation: stop starting watched-run reads after half the processing budget, retaining unvisited due watches for the next poll. Full local reliability suite passes: 21 tests / 105 assertions. Prior turn was verified recovery-test progress. Independent stale-feed monitoring and hosted acceptance remain pending.
- Trigger recovery acceptance now verifies committed first-page evidence, preserved pagination/window after HTTP 429, no provider requests during a 120-second cooldown, and resumption at the saved cursor with the original watermark. Focused local-MySQL test passes with 11 assertions. Prior turn was progress (scheduler/config implementation). Remaining Trigger concerns include watch/discovery budget fairness, independent freshness reporting, and authorized hosted acceptance; checklist 5 stays open.
- Trigger production-only scheduler and validated source configuration are now implemented, disabled by default. Four scheduler-boundary tests / 18 assertions pass (inert disabled/nonproduction execution, whole-batch validation, task/credential binding, sanitized failures and batch budget). Registration runbook added. Prior continuation was verified progress. Checklist 5 remains open for recovery/hosted acceptance and stale-watch monitoring; implementation remains 10% overall.
- Trigger poller now reconciles due watches before paginated discovery, persists each page before checkpoint advancement, and postpones unavailable watches while continuing other runs. Provider rate limits defer the source cursor. New local integration test verifies a failure two days after discovery alongside a 404 watch. Full local suite: 19 tests / 90 assertions pass; DB typecheck and scoped Biome pass. Trigger scheduling, stale-watch escalation, and additional recovery acceptance remain pending; checklist 5 remains open. User requested a light-effort continuation.
- Current checkout: `/Users/M1PRO/Documents/code/_turbo/gnd`, branch `master`.
- Many unrelated active changes exist; do not stage or overwrite them.
- Public test boundaries explicitly confirmed by user: provider ingestion → incident state; incident → GitHub/Slack delivery; incident/release → verified resolution.
- Existing reusable transport: `apps/api/src/utils/bug-report-issue.ts`.
- Shared policy will live under `packages/observability/src/reliability`, DB operations under `packages/db`, thin transport routes in `apps/api`, scheduling in existing Trigger jobs. No new queue runtime is required.
- The initial goal continuation produced authoritative repository evidence and this canonical implementation task; classified as progress.
- Intake contract: 7 focused tests / 20 assertions pass; observability package typecheck passes. Red/green evidence recorded for initial intake, severity policy, and malformed occurrence dates.
- Intake is exported from the server-only `@gnd/observability/reliability` subpath; the browser/mobile root export is unchanged. It prepares scoped identities and reconstructed evidence consumed by durable ingestion.
- Live integrations remain inactive.
- Six reliability models generated and pushed to verified local MySQL. `db:migrate` reached the database with escalation but exited 130 on broad schema/history drift; no reset or migration-history rewrite was performed. Checklist 3 remains unchecked despite local query success.
- Four local-MySQL integration tests / 13 assertions pass: concurrent duplicate delivery, informational-to-actionable transition, out-of-order bounds/severity, and conflicting identity rejection. Fixtures are namespaced and cleaned up. DB package typecheck passes.
- Workspace dependency added from DB to observability; Bun lockfile updated with that one dependency. Existing configVersion retained after the older installed Bun removed it during generation.
- Focused Biome check passes for all five new reliability TypeScript files after applying formatting-only edits to these files. Code review and final validation remain pending. No commit yet.
- Continuation audit: prior turn is progress (implemented and tested intake/persistence). Current files confirm that state. Migration gate remains unresolved, so independent delivery work proceeds without marking checklist 3 complete.
- Delivery ledger now serializes claims across revisions, rejects stale lease settlement, preserves remote receipts, and holds expired sends as uncertain. Matching-marker receipt recovery and integration health queries are available internally; provider verification/dispatch and operator recovery are not yet wired.
- Retry-budget red/green tests caught both unlimited retries and budget reset through fresh revisions. The consecutive budget now survives superseding evidence and stops after five failed attempts; failed streams block newer claims. Eleven local-MySQL tests / 31 assertions pass. Checklist 9 stays open until the remaining reconciliation/recovery integration is implemented.
- Continuation revalidated the current master checkout and new reliability query files. Prior turn classified as progress. Next vertical slice is mapped-principal acknowledgement/assign-to-self with stale revision rejection and atomic audit/outbound intents; transport identity mapping and snooze expiry remain separate incomplete requirements.
- Implemented internal acknowledgement and assign-to-self actions with service-scoped principals, locked revision checks, actor-bound duplicate request handling, and atomic audit/delivery intents. Expanded local suite passes 13 tests / 46 assertions; DB typecheck and focused Biome pass. No external action route or identity resolver is active. Checklist 7 remains open for completing ownership configuration and broader action policy; work is uncommitted and final code review remains pending.
- Sentry integration-platform docs verified on 2026-09-09: webhook signature uses client-secret HMAC-SHA256, payload has installation UUID, resource `event_alert`/action `triggered` carries `data.event`, and response target is one second. The issue-alert example uses `datetime` with six fractional digits, numeric project ID, `event_id`, and `issue_id`; adapter must normalize timestamp precision and enforce installation/project/environment scope. Service hooks are a distinct payload/configuration contract and must not be silently conflated. Source: https://docs.sentry.io/integrations/integration-platform/webhooks/ and its issue-alerts page. These observations change the next adapter implementation; no provider has been connected.
- Current continuation classified prior turn as progress and revalidated master plus intake source. Added `prepareSentryAlert` for integration-platform issue alerts: raw-byte HMAC with constant-time comparison, 1 MiB payload bound, installation/project/production checks, fractional timestamp normalization, and reconstructed safe intake. Four adapter tests / 15 assertions pass; signed redelivery through local DB is covered in the expanded 14-test / 49-assertion integration suite. Observability typecheck passes. HTTP route, configured registration, one-second response evidence, and bounded reconciliation remain incomplete; checklist 4 stays open.
- Next continuation revalidated master and API structure, classified prior work as progress, and mounted the Sentry POST handler with server-owned registration resolution. Five HTTP/config tests / 13 assertions pass, including a deferred commit proving no early acknowledgement, streamed body size enforcement, invalid-signature rejection before persistence, and retryable storage failure. Scoped Biome passes. No environment values were changed; hosted timing and reconciliation still prevent closing checklist 4.
- API wiring exposed base-vs-extended Prisma typing. Reliability queries now use GND's Database/TransactionClient types and the integration harness uses `createDatabaseClient`; all 14 integration tests / 49 assertions pass with the production extension. API typecheck now reports only `packages/sales/src/copy-sales.ts:521` (nullable string assigned to string), outside this task; reliability wiring errors are resolved. Scoped Biome passes. Added a registration runbook with placeholder-only configuration; final review and commit remain pending.
- Current continuation revalidated master and the cursor schema, and classified prior work as progress. Added row-locked cursor claims, resumable fixed-window pagination, terminal-page watermark commit, stale/page-revision rejection, and provider cooldowns. The initial competing-worker test failed; explicit lock/lease checking resolved it. Fifteen local-MySQL tests / 64 assertions and DB typecheck pass. Provider pagination loops, their page persistence ordering, and stale-feed alerting remain to be implemented; no broader reconciliation completion is claimed.
- Next continuation revalidated master and jobs layout, classified prior work as progress, and verified Sentry's project error-event API and Link pagination docs. Added the bounded authenticated read adapter and Sentry polling orchestration. Three reader tests / 11 assertions and 16 local-MySQL integration tests / 74 assertions pass, including rate-limited second-page recovery with the original window, persisted first-page evidence, and page-budget deferral. Observability typecheck and scoped Biome pass. Trigger task registration/configuration, historical sweep policy, hosted acceptance, and stale-feed alerting remain open; checklist 4 stays incomplete.
- Jobs typecheck remains non-green: 33 diagnostics in sales copying and shared UI React types, none in reliability files. Full output retained at `/tmp/gnd-reliability-jobs-typecheck.log`; no unrelated fixes were applied. Scope remains uncommitted and final review is outstanding.
- Current continuation revalidated master, Trigger configuration, and existing schedule conventions; prior turn classified as progress. Added production-only five-minute task definition, default-off polling gate, separately referenced read credentials, duplicate-scope validation, and bounded multi-source execution. Five scheduler tests / 12 assertions pass. Jobs typecheck again reports the same 33 sales/UI diagnostics and none in reliability code. Runbook includes exact placeholder-only polling configuration. No deployment or secrets changes occurred. Historical sweep, stale-feed escalation, and hosted verification remain open; checklist 4 is not yet complete.
- Next continuation revalidated master and cursor/schedule implementations; prior turn classified as progress. Added an hourly seven-day historical sweep with an independent cursor and fresh lookback after completed scans. The rescan test initially failed with the incremental behavior and passes with explicit replay-window mode. Seventeen database integration tests / 79 assertions and six scheduler tests / 14 assertions pass; scoped Biome passes. Hosted verification and independent stale-feed escalation remain outstanding; no deployment or configuration changes were made.
- Historical-sweep jobs typecheck completed with the same 33 sales/UI dependency errors and no reliability diagnostics. Work remains uncommitted and final review remains pending.
- Current continuation revalidated master, Trigger list/retrieve docs, and installed SDK status schemas. Prior turn classified as progress. Added safe Trigger run normalization and durable watch primitives; an old executing run remains due until terminal evidence commits, and stale provider revisions cannot rewind it. Added nullable providerUpdatedAt to the watch model. Local generation/push succeeded; migrate again exited 130 on existing history drift without a reset. Provider HTTP discovery/watch loops remain incomplete.
- Trigger validation: four normalizer tests / 21 assertions, 18 local-MySQL integration tests / 83 assertions, and observability/DB typechecks pass. Scoped lint completed after import sorting. Tests cover platform failures, fallback ownership operation, intentional/unclassified cancellation, and a two-day-old run failing later. Updated schema/migration/feature notes. No external provider state was changed; final review and commit remain outstanding.
- Current continuation revalidated master and installed Trigger core 4.5.9 HTTP serialization: list uses `/api/v1/runs`, `page[size]`/`page[after]`, and epoch-millisecond creation bounds; retrieve uses `/api/v3/runs/:id`. Prior turn classified as progress. Added bounded HTTP discovery/retrieval adapters with explicit pagination validation, fixed host, no redirects, production-key prefix guard, timeout/body limits, and safe rate-limit errors. Three reader tests / 12 assertions, observability typecheck, and scoped Biome pass. Combined polling/watch orchestration and live source verification remain open. No network call used a real provider token.
