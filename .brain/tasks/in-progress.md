# In Progress

- 2026-07-22 execution note: the responsive full-width Tables-2 core contract
  was tracked here during implementation and moved to
  `.brain/tasks/done.md` after focused tests, configuration audit, and canonical
  responsive browser validation completed. See
  `.brain/features/tables-2-responsive-full-width.md` and ADR-019.

## Purpose
Tracks the active work queue. Keep this focused and execution-ready.

## Current Focus
- [ ] Storefront production cutover: implementation, isolated migration
  verification, and user-confirmed development/production schema pushes are
  complete. Remaining gates are production catalog/content publication, real
  Square/email rehearsal, representative Door/Moulding/Shelf parity fixtures,
  and monitored DNS/redirect cutover. Customer profile pricing and scheduled
  targeted promotions are implemented, locally browser-verified, and deployed
  to the production schema after an additive-only preflight and empty post-push
  diff; `@gnd/storefront` typecheck and production build pass. See
  `.brain/features/storefront-ecommerce-replacement.md` and
  `.brain/features/storefront-profile-pricing-promotions.md`.
- [ ] Dealership Program Expansion and Recruitment: branding ZIP/cache
  freshness, private/shared dealer customers, immutable direct-ship snapshots,
  Super Admin campaigns, sales-email banners, secure applications,
  approval/password activation, and suspension/reactivation are implemented.
  Focused validation is green (70 tests / 178 assertions). Remaining release
  gate: apply the Prisma schema, restart services, and complete ticket 9's
  campaign-to-reactivation browser proof. Local migration currently fails with
  Prisma `P1001` for `127.0.0.1:3307`. (DB + API + Email + Web + Dealership +
  Validation)
- [ ] New Sales Form Parity And History Readiness: same-order parity was browser-checked on `08893LM` before any new-sale creation; customer profile repricing now includes grouped moulding rows, tax group/rate, Bifold, moulding, totals, calculated component sales-cost display, compact shadcn HPT estimate breakdown, grouped-service row-granular tax precedence, deferred profile-option repricing, default V2 shelf category paths, first-size HPT add-size availability, picker moulding-calculator outside-click dismissal, and hosted calculator price-per-LF/default-refresh semantics, with a user-accepted one-cent historical interior-door rounding variance (`$233.69` new/persisted vs `$233.70` legacy). HPT composed-component surcharge fallback now reads selected-component snapshots when persisted step prices are missing. Actual history listing, lazy snapshot preview, read-only amber preview banner, local restore, ID sanitization, and blue restored-version banner are implemented and covered by focused tests. Remaining gate, deferred by user request on 2026-07-18: browser-accept preview/restore/banner behavior on an existing order with `order-hx` snapshots before declaring the new sales form fully ready; picker profile/dependency pricing, tax-code/profile transitions, moulding calculator/price-summary flow, shelf category/product flow, HPT size/total flows, and composed HPT totals still need authenticated browser proof. (Web + API + Sales Domain + Validation)
- [ ] Inventory System Correctness Cutover: implement `brain/plans/2026-07-01-inventory-system-correctness-cutover-plan.md`; completed slices include the Phase 0 invariant matrix (`brain/reports/2026-07-01-inventory-correctness-invariant-matrix.md`), terminal fulfilled/cancelled read-only enforcement, active-tab Inventory/Inbounds loading, inbound create/assign parent-sale guards, route input validation and confirmed-write proof slices, Phase 8 repair-path audit/evidence runner/repair runner, reviewed stale/componentless repair apply evidence, sync stale-cleanup timestamp precision hardening, shipment/allocation classification evidence, missing-sales scope classification evidence, scoped active/order missing-sales batch evidence, materializable/mapping-blocked backfill classification, materializable active/order backfill apply evidence, zero-component componentless review evidence, zero-component source-shape classification evidence, HPT door component fallback, reviewed zero-component repair apply evidence, and eleven additional reviewed materializable backfill applies; repairs are stopped by user request, so no further repair dry-runs or applies should run unless explicitly resumed. The 2026-07-23 read-only refresh no longer reproduces the cleaner July 1 post-repair checkpoint: current local evidence is `21745` missing sales, `50` stale lines, `5` componentless lines, `281` reconciliation drifts, `14` skipped comparisons, `hasMore=true`, and next cursor `200`. Before any repair resumes, investigate the local-baseline regression and re-review current candidates; non-active/mapping-blocked scope and operator shipment truth still require decisions. Remaining gates include Phase 1-7 operator proof gaps, Phase 9 UI polish, Phase 10 browser proof matrix, and Phase 11 release gates before any broad cutover claim (Inventory + API + UI + Validation)
  - Ledger rule: this current-work item stays open until the cutover plan's Phase 11 release acceptance is recorded; `brain/tasks/done.md` entries are completed slice evidence, not full cutover completion.
  - Intake status: source intake `brain/intake/2026-06-15-inventory-cutover-pending-scope.md` and related workflow intake `brain/intake/2026-06-22-sales-overview-inventory-workflows.md` are both present; user-requested pending-gates consolidation intake `brain/intake/2026-07-01-inventory-correctness-pending-gates.md` now lists all remaining gates without expanding scope or resuming repairs.
  - Latest ledger correction: 2026-07-01 documentation-only alignment updated the plan, roadmap, in-progress ledger, progress log, done ledger, and reconciliation evidence after eleven additional reviewed materializable active/order backfill applies while keeping the full cutover open and recording that repairs are stopped by user request.
  - Latest evidence refresh: 2026-07-23 read-only evidence reports
    `needs_backfill` with `21745` missing sales, `5` componentless lines, `50`
    stale lines, `281` total drift, `14` skipped comparisons, `hasMore=true`,
    and next cursor `200`; no emitted repair payload was run.
  - Pending phase order: Phase 8 clean reconciliation, remaining Phase 1-7 operator proof gaps, Phase 9 UI polish, Phase 10 browser proof matrix, then Phase 11 release gates.
- [ ] Sales Overview Inventory Demand Projection: implement `brain/plans/2026-06-22-feature-sales-overview-inventory-demand-projection.md`; first slice extends `inventories.salesInventoryOverview` with grouped invoice-item inventory rows, stock totals, pending quantities, costs, statuses, and action eligibility; the Inventory tab now also has a lightweight linked-inbound count contract and guarded availability path, with browser validation and any fixture-specific follow-up still open (API + Inventory + Validation)
- [ ] Sales Overview Inventory Tab UI: implement `brain/plans/2026-06-22-ux-ui-sales-overview-inventory-tab.md`; the active Inventory tab now belongs to the canonical Sales Overview sheet, using the grouped projection with read-only/disabled action affordances; the Inbounds segment has pending-stock CTAs, accurate pre-open counts, and collapsible shipment rows, with browser validation, canonical module renaming/dynamic loading, and mutation-ready polish still open (UI + Validation)
- [ ] Mobile Sales Form Foundation And Customer Flow: implement `brain/plans/2026-06-18-ux-ui-mobile-sales-form-foundation-customer-flow.md` from handoff `brain/handoffs/ready/2026-06-18-mobile-sales-form-foundation-customer-flow-handoff.md`; scope includes Sales/Quote start sheet, type-aware recent customers, component UID subtitle cleanup, and shared floating-action architecture (Expo + UI + API if needed + Validation). Mobile save attempts now also carry development-only diagnostic request ids through the shared save contract; manual device QA remains open.
- [ ] Mobile Custom Component Parity: implement `brain/plans/2026-06-18-ux-ui-mobile-custom-component-parity.md` from handoff `brain/handoffs/ready/2026-06-18-mobile-custom-component-parity-handoff.md`; scope includes morphable custom component sheet, search/create/update/select behavior, floating custom button, and selected-custom-first component list parity (Expo + UI + Inventory API + Validation)
- [ ] Mobile Door Size And House Package Parity: implement `brain/plans/2026-06-18-ux-ui-mobile-door-hpt-parity.md` from handoff `brain/handoffs/ready/2026-06-18-mobile-door-hpt-parity-handoff.md`; scope includes door-size next-step fix, multi-select proceed, and full mobile HPT parity with website new sales form (Expo + UI + Sales Form Core + Validation)
- [ ] Mobile Moulding Multi Select Parity: implement `brain/plans/2026-06-18-ux-ui-mobile-moulding-parity.md` from handoff `brain/handoffs/ready/2026-06-18-mobile-moulding-parity-handoff.md`; scope includes moulding multi-select, floating proceed, grouped row editing, and `line.meta.mouldingRows` persistence parity (Expo + UI + Sales Form Core + Validation)
- [ ] Mobile Shelf Items Parity: implement `brain/plans/2026-06-18-ux-ui-mobile-shelf-items-parity.md` from handoff `brain/handoffs/ready/2026-06-18-mobile-shelf-items-parity-handoff.md`; scope includes shelf category/product search, sections, rows, totals, and canonical `shelfItems` payload parity (Expo + UI + Sales Form Core + Validation)
- [ ] Tables-2 sales-orders standard migration: migrate route-level table pages to the current `components/tables-2/sales-orders` standard by adding only `components/tables-2/<domain>/*`, reusing existing queries, filter params, and page headers, leaving `components/tables-2/core` unchanged, and removing old unused table implementations only after import/route validation. Completed slices: canonical `/sales-book/orders`, `/sales-book/orders/bin`, canonical `/sales-book/quotes`, `/sales-book/quotes/bin`, canonical `/sales-book/customers`, canonical `/sales-book/dealers`, canonical `/sales-book/dispatch`, `/sales-book/dispatch-admin`, `/sales-book/dispatch-task`, `/sales-book/inbound-management`, `/sales-book/inbounds`, canonical `/sales-book/accounting`, `/sales-book/accounting/resolution-center`, sales statistics/product report (`/sales-book/top-selling-products`, `/product-report`), `/sales-book/shelf-items`, `/sales-book/emails`, `/task-events/diagnostics`, `/task-events/[eventName]`, inventory products/components (`/inventory`, `/inventory/components`), inventory categories (`/inventory/categories`), inventory imports diagnostics (`/inventory/imports`), inventory variants (`/inventory/variants`), inventory allocations (`/inventory/allocations`), inventory inbounds (`/inventory/inbounds`), inventory backorders (`/inventory/backorders`), inventory partial shipments (`/inventory/partial-shipments`), inventory dispatch mode (`/inventory/dispatch-mode`), inventory production plan (`/inventory/production-plan`), inventory kind review (`/inventory/review`), inventory stock audit (`/inventory/stocks`), inventory suppliers (`/inventory/suppliers`), inventory item dashboard (`/inventory/[id]`), community builders (`/community/builders`), community templates (`/community/templates`), customer services (`/community/customer-services`), unit invoices (`/community/unit-invoices`), employees (`/hrm/employees`, `/hrm/employees/v2` list), roles/profile sheet tabs, role form permissions, employee form permissions, job overview scope, Customer Overview transactions, Transaction Overview modal applications/payments, Customer Overview Pay Portal/Sales List, legacy sales-form Door Suppliers, contractor jobs (`/hrm/contractors/jobs`), community projects (`/community/projects`), project units (`/community/project-units`), unit productions (`/community/unit-productions`), `/sales-rep` recent sales/recent quotes and commission-tab embeds, `/sales-rep/design` Recent Activity, Sales Production (`tables-2/sales-production`) including the live v2 worker/admin routes `/production/dashboard/v2` and `/sales-book/productions/v2`, Project Overview embeds for Units, Production, Invoices, and Jobs, `/jobs-dashboard/jobs-list`, `/contractors/jobs/payments`, `/contractors/jobs/payment-dashboard` recent payments and Ready for payout contractor queue, `/contractors/jobs/payment-portal` payable jobs, `/site-actions`, `/settings/short-links`, `/settings/master-password-logins`, `/settings/profile` active sessions, `/settings/notification-channels/v2`, legacy Square payment fallback multi-order checkout table, and `/sales/packing-list`. Latest restarted slices removed the failed shared `PageStickyHeader` direction and now follow the Sales Orders route/table shell directly with compact, content-tailored columns; `/sales-rep` consumes shared order/quote tables plus dedicated commission/commission-payment tables in embedded single-page mode, `/sales-rep/design` Recent Activity uses `tables-2/sales-rep-design-activity` with compact sticky Order columns, Sales Production live v2 routes now use `ProductionWorkspace` plus compact worker/admin `tables-2/sales-production` columns, flat queue table surface, sticky Actions, and Sales Orders-style table height, Project Overview no longer imports legacy contractor/unit invoice/unit production tables, the worker jobs dashboard list consumes the restarted contractor-jobs table with compact worker columns and guarded empty-state submission, contractor payout history now uses `tables-2/contractor-payouts` with selected print-report bottom bar, the contractor payment dashboard uses `tables-2/payment-dashboard-contractors` for the Ready for payout queue with compact Contractor/Insurance/Jobs/Recent Project/Total Pay/Actions widths and `tables-2/payment-dashboard-recent-payments` for recent payments with compact Payout/Paid To/Jobs/Method/Paid By/Amount/Actions widths, the contractor payment portal payable jobs list now uses `tables-2/payment-portal-jobs` with controlled row selection, compact sticky Select/Job/Actions columns, and preserved review/print/payout actions, Site Actions now uses `tables-2/site-actions` with compact audit-log columns plus hardened cursor/sort schema, Short Links now uses `tables-2/short-links` with compact URL/admin columns plus hardened cursor/sort schema, the Employees v2 list now uses `tables-2/employees` instead of the legacy employee table, the roles/profile sheet tabs now use `tables-2/roles` and `tables-2/employee-profiles` with compact 56px rows and corrected profile edit/delete wiring, the role form permission editor now uses `tables-2/role-form-permissions` with compact 48px rows and sticky Permission columns, the employee form permission editor now uses `tables-2/employee-form-permissions` with compact 48px rows and sticky Permission columns, Customer Overview transactions now use `tables-2/customer-transactions` with compact sticky Date/Actions columns and modal row-open behavior, the global Transaction Overview modal now uses `tables-2/transaction-overview-applications` and `tables-2/transaction-overview-payments` with compact sticky Invoice/Date columns and tight modal-fit widths, Customer Overview Pay Portal/Sales List now use `tables-2/customer-pay-portal` and `tables-2/customer-sales-list` with compact 48px rows, tight payment/quote widths, and preserved selection/payment/list behavior, legacy sales-form Door Suppliers now uses `tables-2/door-suppliers` with compact 48px rows and tight Selected/Supplier/Actions widths, the Sales Resolution Center now uses `tables-2/sales-resolution` with fixed-height compact rows plus selected payment-review panels below the table, Inventory Allocations now uses `tables-2/inventory-allocations` with compact sticky allocation review columns and selected-row bulk approval, Inventory Variants now uses `tables-2/inventory-variants` with compact sticky variant/action columns while preserving workspace metrics and filters, Inventory Inbounds now uses `tables-2/inventory-inbounds` for the primary shipment queue while preserving selected shipment receiving workflows, Inventory Backorders now uses `tables-2/inventory-backorders` with compact sticky order/action columns while preserving queue metrics, filters, and ship-available actions, Inventory Partial Shipments now uses `tables-2/inventory-partial-shipments` with compact sticky order/action columns while preserving hold/unhold and ship-available actions, Inventory Dispatch Mode now uses `tables-2/inventory-dispatch-mode` with compact sticky order/action columns while preserving whole-line and allocation-specific assign/pack/fulfill/release actions, Inventory Production Plan now uses `tables-2/inventory-production-plan` with compact sticky component/action columns while preserving readiness filters, supplier/status summaries, and the print-production action, Inventory Kind Review now uses `tables-2/inventory-kind-review` with compact sticky item/action columns while preserving summary cards and the product-kind backfill action, Inventory Stock Audit now uses `tables-2/inventory-stock-audit` with compact sticky audit-category columns while preserving the manual stock adjustment form and refreshing audit counts after adjustment, Inventory Suppliers now uses `tables-2/inventory-suppliers` with compact sticky Supplier columns, tailored Supplier/Contact/Address/Actions widths, preserved search/create/edit/delete/default/sync/review behavior, and supplier-directory invalidation after mutations, Inventory Item Dashboard now uses `tables-2/inventory-item-dashboard` for variants, stock, movements, inbound demand, allocations, and related sales/quotes while preserving item metrics and top-sales analytics, Community Builders now uses the restarted route shell with `batchPrefetch`, `ScrollableContent`, `useScrollHeader`, DnD, draggable/sortable headers, and tighter Builder/count/action widths, Packing List now uses `tables-2/packing-list` with compact sticky Order/Actions columns while preserving current/completed/admin-cancelled tabs, packing-slip open, and admin status actions, Shelf Items now uses `tables-2/shelf-items` with compact sticky Product/Actions columns, tailored Product/Category/Price/Status/Actions widths, preserved product/category management, Sales Email Ledger now uses `tables-2/sales-email-ledger` with compact sticky Status/Actions columns, tailored delivery-audit widths, existing search/status pagination, refresh, and Super Admin resend behavior, Task Run Diagnostics now uses `tables-2/task-run-diagnostics` with compact sticky Status/Actions columns, tailored diagnostics widths, existing search/status pagination, and Super Admin mark-reviewed behavior, Task Event History now uses `tables-2/task-event-history` with compact sticky Time, tailored Time/Value/Trigger/Meta widths, existing run controls, sales-order filter editor, manual payment-report run flow, and task-event list invalidation behavior, Master Password Logins now uses `tables-2/master-password-logins` with compact sticky User/Actions columns, tailored audit widths, existing search/platform/show-cleared pagination, refresh, clear-filtered, and row-level clear behavior, Settings Profile Active Sessions now uses `tables-2/user-logged-in-devices` with compact sticky Device/Actions columns, Settings Notification Channels now uses `tables-2/notification-channels` with compact sticky Channel/Actions columns, legacy Square payment fallback multi-order checkout now uses `tables-2/legacy-square-payment-orders` with compact sticky Invoice and tight Invoice/Billing/Due widths, and the Sales Rep commission tab now uses `tables-2/sales-rep-commission-payments` plus `tables-2/sales-rep-commissions` with compact sticky Payment/Commission columns while removing the old helper table/demo-row commission surfaces. The old shared-header rollout test now audits restarted pages against `PageStickyHeader`, legacy table imports, `@gnd/ui/data-table`, and manual `fetchInfiniteQuery` regressions instead of requiring the failed wrapper. Remaining next slices include admin/settings tables and other operations tables (`brain/plans/2026-06-16-orders-v2-table-standard-migration.md`) (UI + Validation + Cleanup)
  - Latest slice: 2026-07-17 `/inventory/stocks` density/width tuning now brings the Inventory Stock Audit table closer to Sales Orders/Midday invoices while preserving the existing stock operations route shell, `stockAuditVerificationReport` data source, manual stock adjustment form, `adjustInventoryStock` mutation, audit-count invalidation, and table-core contract. Category/Expected/Movements/Logs/Change/Status widths are tightened (`160/260/190`, `190/300/220`, `104/150/118`, `92/132/104`, `104/150/118`, `104/150/118`), Change/Status badges use compact `h-6` sizing, and `TABLE_CONFIGS["inventory-stock-audit"].rowHeight` remains `56`. Validation passed with focused Stock Audit parity tests (3 / 32), full `apps/www/src/components/tables-2` tests (306 / 2570), focused Biome, touched-file typecheck grep with no Stock Audit/table-config diagnostics, clean `components/tables-2/core` diff, tracked `git diff --check`, direct local HTTP route smoke returning `200`, and authenticated browser proof confirming `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 63`), no document-level horizontal overflow, and no horizontal table overflow at the current desktop width because the compact columns fit the container. Portless HTTPS timed out after 60s with zero bytes, and `https://gndprodesk.localhost:3011` was not listening.
  - Latest validation follow-up: 2026-07-17 `/community/project-units` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated Codex in-app browser validation loaded the Project Units table with compact `64px` rows, a `45px` header, sticky Select/Lot-Block/Actions cells, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 3885 -> 3955`, `clientHeight 653 -> 723` after header offset), table-owned horizontal scroll (`scrollLeft 0 -> 236`, `scrollWidth 1604`, `clientWidth 1353 -> 1368`), `--header-offset: 70px`, and no document-level horizontal overflow. The claimed in-app browser tab was restored to its original Sales Orders URL after proof.
  - Latest validation follow-up: 2026-07-17 `/community/unit-productions` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and after route warmup authenticated browser validation loaded `65,096` production tasks with compact `64px` rows, a `45px` header, sticky Select/Due Date columns, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 3885 -> 3955`, `clientHeight 389 -> 459` after header offset), table-owned horizontal scroll (`scrollLeft 0 -> 138`, `scrollWidth 1284`, `clientWidth 1146`), `--header-offset: 70px`, and no document-level horizontal overflow. The first portless HTTPS attempt timed out before warmup, but the warmed authenticated browser route rendered successfully.
  - Latest validation follow-up: 2026-07-17 `/community/customer-services` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation loaded work-order rows with compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 2285`, `clientHeight 299`), and no document-level horizontal overflow. The compact columns fit the current desktop table container exactly (`scrollWidth 1131`, `clientWidth 1131`), so horizontal overflow was not present to scroll.
  - Latest validation follow-up: 2026-07-17 `/hrm/document-approvals` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation loaded the approval queue with compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 1837`, `clientHeight 419`), and no document-level horizontal overflow. The compact columns fit the current desktop table container exactly (`scrollWidth 1144`, `clientWidth 1144`), so horizontal overflow was not present to scroll.
  - Latest validation follow-up: 2026-07-17 `/inventory/backorders` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation loaded 100 queue lines with compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 5645`, `clientHeight 359`), table-owned horizontal scroll (`scrollLeft 0 -> 260`, `scrollWidth 1490`, `clientWidth 1131`), and no document-level horizontal overflow.
  - Latest validation follow-up: 2026-07-17 `/inventory/production-plan` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation confirmed `56px` rows, a `45px` header, table-owned horizontal scroll (`scrollLeft 0 -> 260`, `scrollWidth 1486`, `clientWidth 1131`), and no document-level horizontal overflow. Current authenticated data had only two production component rows, so vertical row-scroll movement could not be exercised.
  - Latest validation follow-up: 2026-07-17 `/sales-book/emails` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation loaded the Sales Email Ledger with temporary local proof rows after confirming the active DB was local `127.0.0.1:3307/gnd-prisma2`. It confirmed compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 1515`, `clientHeight 439`), table-owned horizontal scroll (`scrollLeft 0 -> 70`, `scrollWidth 1216`, `clientWidth 1146`), no document-level horizontal overflow, and no login/runtime error. The `32` proof rows were deleted afterward and `SalesEmailAttempt` returned to `0` rows.
  - Latest validation follow-up: 2026-07-17 `/task-events/diagnostics` browser proof gap is closed for the density pass. Direct local HTTP route smoke returned `200`, and authenticated browser validation loaded the Task Diagnostics table with temporary local proof rows after confirming the active DB was local `127.0.0.1:3307/gnd-prisma2`. It confirmed compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 2915`, `clientHeight 439`), table-owned horizontal scroll (`scrollLeft 0 -> 148`, `scrollWidth 1294`, `clientWidth 1146`), no document-level horizontal overflow, and no login/runtime error. The `65` proof rows were deleted afterward and `TaskRunDiagnostic` returned to `0` rows.
  - Latest validation follow-up: 2026-07-17 `/settings/notification-channels/v2` browser proof gap is closed for the density pass. HTTPS/direct HTTP route smokes returned `200` after browser warmup, and authenticated browser validation loaded `57` notification channels with compact `56px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 650`, `scrollHeight 3237`, `clientHeight 496`), table-owned horizontal scroll (`scrollLeft 0 -> 260`, `scrollWidth 1014`, `clientWidth 556`), `--header-offset: 70px` after scroll, and no document-level horizontal overflow.
  - Previous slice: 2026-07-17 `/inventory/review` density/width tuning now brings the Inventory Kind Review table closer to Sales Orders/Midday invoices while preserving the existing kind-review route shell, `inventoryProductKindReview` data source, summary cards, heuristic copy, `backfillInventoryProductKinds` action, table-owned infinite scroll, and table-core contract. Item/Category/Current/Suggested/Evidence/Counts/Status/Actions widths are tightened (`200/380/250`, `150/260/180`, `104/150/118`, `112/170/128`, `140/220/160`, `120/170/136`, `112/170/128`, `84/112/96`), kind/status badges use compact `h-6` sizing, row action buttons are compact icon-only controls, and `TABLE_CONFIGS["inventory-kind-review"].rowHeight` is now `56`. Validation passed with focused Kind Review parity tests (3 / 25), full `apps/www/src/components/tables-2` tests (306 / 2566), focused Biome, touched-file typecheck grep with no Kind Review/table-config diagnostics, clean `components/tables-2/core` diff, tracked `git diff --check`, and authenticated browser proof confirming `56px` rows, a `45px` header, table-owned vertical/horizontal scroll (`scrollTop 0 -> 650`, `scrollLeft 0 -> 50`), and no document-level horizontal overflow. HTTPS route smoke timed out after 60s with zero bytes, but the authenticated browser rendered the route.
  - Previous slice: 2026-07-17 `/inventory/dispatch-mode` density/width tuning now brings the Inventory Dispatch Mode queue closer to Sales Orders/Midday invoices while preserving the existing dispatch-mode route shell, `salesPartialShipmentQueue` data source, summary cards, status filters, Partial Shipments/Backorders links, row overview navigation, whole-line assign/pack/fulfill/release workflows, allocation-specific row-menu workflows, invalidation behavior, and table-core contract. Order/Line/Status/Qty/Available/Allocations/Blockers/Actions widths are tightened (`160/280/190`, `200/360/240`, `124/190/140`, `170/260/190`, `104/150/118`, `190/320/220`, `200/340/240`, `136/180/150`), blocker previews show one primary blocker plus compact overflow count, row badges/actions use tighter sizing, and `TABLE_CONFIGS["inventory-dispatch-mode"].rowHeight` is now `56`. Validation passed with focused Dispatch Mode parity tests (3 / 26), full `apps/www/src/components/tables-2` tests (306 / 2558), focused Biome, touched-file typecheck grep with no Dispatch Mode/table-config diagnostics, HTTPS route smoke returning `200` in 33.6s, clean `components/tables-2/core` diff, tracked `git diff --check`, and authenticated browser proof confirming `56px` row height, a `45px` header, table-owned horizontal scroll (`scrollLeft 0 -> 102`), and no document-level horizontal overflow. Current authenticated data had only one dispatch row, so vertical row-scroll movement could not be exercised.
  - Previous slice: 2026-07-17 `/inventory/partial-shipments` density/width tuning now brings the Partial Shipments queue closer to Sales Orders/Midday invoices while preserving the existing partial shipment queue query, summary cards, status filters, Backorders link, hold/unhold mutation, row overview navigation, Ship Available mutation, and table-core contract. Order/Line/Status/Fulfillment/Available/Holdback/Blockers/Hold/Actions widths are tightened (`160/280/190`, `200/360/240`, `124/190/140`, `170/260/190`, `104/150/118`, `112/170/128`, `200/340/240`, `82/120/94`, `124/160/136`), blocker previews show one primary blocker plus compact overflow count, row badges/actions use tighter sizing, and `TABLE_CONFIGS["inventory-partial-shipments"].rowHeight` is now `56`. Validation passed with focused Partial Shipments parity tests (4 / 25), full `apps/www/src/components/tables-2` tests (306 / 2553), focused Biome, touched-file typecheck grep with no Partial Shipments/table-config diagnostics, direct local GET route smoke returning `200` in 20.6s, clean `components/tables-2/core` diff, touched-file `git diff --check`, and authenticated browser proof confirming `56px` rows, a `45px` header, table-owned vertical/horizontal scroll (`scrollTop 0 -> 650`, `scrollLeft 0 -> 220`), and no document-level horizontal overflow.
  - Previous slice: 2026-07-17 `/inventory/backorders` density/width tuning now brings the Backorders queue closer to Sales Orders/Midday invoices while preserving the existing backorder queue query, status filters, summary cards, Print backorders, Allocate received, partial-shipment link, row overview navigation, Ship Available mutation, and table-core contract. Order/Line/Status/Fulfillment/Available/Backorder/Received/Blockers/Actions widths are tightened (`160/280/190`, `200/360/240`, `124/190/140`, `170/260/190`, `104/150/118`, `112/170/128`, `96/140/108`, `200/340/240`, `124/160/136`), blocker previews show one primary blocker plus compact overflow count, row badges/actions use tighter sizing, and `TABLE_CONFIGS["inventory-backorders"].rowHeight` is now `56`. Validation passed with focused Backorders parity tests (3 / 20), full `apps/www/src/components/tables-2` tests (305 / 2547), focused Biome, touched-file typecheck grep with no Backorders/table-config diagnostics, direct local HTTP route smoke returning `200`, clean `components/tables-2/core` diff, and touched-file `git diff --check`; the initial HTTPS proxy route smoke timed out with zero bytes after 25s, but later authenticated browser proof confirmed `56px` rows, a `45px` header, table-owned vertical/horizontal scroll (`scrollTop 0 -> 650`, `scrollLeft 0 -> 260`), and no document-level horizontal overflow.
  - Previous slice: 2026-07-17 `/hrm/document-approvals` density/width tuning now brings the Document Approvals queue closer to Sales Orders/Midday invoices while preserving the existing approvals route shell, `getEmployeeDocumentApprovals` load path, `reviewEmployeeDocument` approve/reject action, Open Review URL-param behavior, document URL opening, and table-core contract. Employee/Document/Status/Dates/Reviewed/Actions widths are tightened (`180/320/220`, `180/340/220`, `104/150/118`, `132/210/154`, `112/170/128`, `156/210/172`), employee avatars are `size-8`, two-line cells use smaller/tighter text, review buttons use compact `h-8` controls, and `TABLE_CONFIGS["document-approvals"].rowHeight` is now `56`. Validation passed with focused Document Approvals parity tests (3 / 41), full `apps/www/src/components/tables-2` tests (305 / 2543), focused Biome, touched-file typecheck grep with no Document Approvals/table-config diagnostics, direct HTTP route smoke returning `200`, clean `components/tables-2/core` diff, touched-file `git diff --check`, and follow-up authenticated browser proof confirming table-owned vertical scroll plus row/header sizing.
  - Previous slice: 2026-07-17 `/community/customer-services` density/width tuning now brings the Customer Service work-order table closer to Sales Orders/Midday invoices while preserving the existing customer-service query/filter/header, summary widgets, chart, work-order sheet params, assignment/status mutations, edit/delete actions, row selection, bottom bar, and table-core contract. Appointment/Customer/Description/Assigned To/Status/Actions widths are tightened (`132/210/154`, `160/280/190`, `220/420/260`, `140/220/160`, `112/170/128`, `88/112/96`), two-line cells use tighter spacing, the assignment combobox uses a compact `h-8` trigger, and `TABLE_CONFIGS["customer-service"].rowHeight` is now `56`. Validation passed with focused Customer Services parity tests (4 / 38), full `apps/www/src/components/tables-2` tests (305 / 2538), focused Biome, touched-file typecheck grep with no Customer Service/table-config diagnostics, direct HTTP route smoke returning `200`, clean `components/tables-2/core` diff, touched-file `git diff --check`, and follow-up authenticated browser proof confirming table-owned vertical scroll plus row/header sizing.
  - Previous slice: 2026-07-17 `/task-events/[eventName]` density/width tuning now brings the Task Event History table closer to Sales Orders/Midday invoices while preserving the existing task-event get/history/update/runNow/runTest contracts, status save, Run Test, Run Now, manual payment-report run, sales-order filter editor, history refetch, and task-event list invalidation behavior. Time/Value/Trigger/Meta widths are tightened (`136/210/156`, `72/112/84`, `96/150/112`, `300/700/460`), metadata previews use bounded/tighter multi-line sections, and `TABLE_CONFIGS["task-event-history"].rowHeight` is now `112`. Validation passed with focused task-event-history migration-parity tests (3 / 37), full `apps/www/src/components/tables-2` tests (305 / 2529), focused Biome, HTTPS/direct route smokes returning `200`, clean `components/tables-2/core` diff, browser proof confirming `112px` rows, a `45px` header, table-owned vertical scroll (`scrollTop 0 -> 600`), no document-level horizontal overflow, and screenshot evidence at `/private/tmp/gnd-task-event-history-table.jpg`; narrow viewport proof at `760px` confirmed table-owned horizontal/vertical scroll (`scrollLeft 0 -> 153`, `scrollTop 0 -> 600`) with screenshot evidence at `/private/tmp/gnd-task-event-history-table-narrow.jpg`.
  - Previous slice: 2026-07-17 `/task-events/diagnostics` density/width tuning now brings the Task Run Diagnostics table closer to Sales Orders/Midday invoices while preserving the existing `taskRunDiagnostics.list` query, search/status/page filters, refresh, Task Events link, Super Admin mark-reviewed behavior, and table-core contract. Status metadata is flattened into one row, Error is tightened to one primary line plus details, Status/Task/Entity/User/Started/Error/Actions widths are tightened (`128/196/148`, `190/360/230`, `150/280/180`, `160/300/200`, `118/180/136`, `220/460/280`, `108/140/120`), and `TABLE_CONFIGS["task-run-diagnostics"].rowHeight` is now `56`. Validation passed with focused task-run-diagnostics migration-parity tests (3 / 37), full `apps/www/src/components/tables-2` tests (305 / 2528), focused Biome, touched-file typecheck scan with no diagnostics, clean touched-file `git diff --check`, clean `components/tables-2/core` diff, and follow-up authenticated browser proof confirming compact row/header sizing plus table-owned vertical/horizontal scroll after the local table blocker was resolved.
  - Previous slice: 2026-07-17 `/sales-book/emails` density/width tuning now brings the Sales Email Ledger table closer to Sales Orders/Midday invoices while preserving the existing `emails.salesEmailAttempts` query, local search/status/page filters, refresh, pagination, Super Admin resend behavior, and table-core contract. Status timestamps are flattened, Status/Recipient/Sales/Subject/Rep/Provider/Actions widths are tightened (`142/210/160`, `180/320/220`, `128/220/150`, `200/420/260`, `128/220/150`, `136/240/160`, `104/132/116`), and `TABLE_CONFIGS["sales-email-ledger"].rowHeight` is now `56`. Validation passed with focused sales-email-ledger migration-parity tests (3 / 39), full `apps/www/src/components/tables-2` tests (305 / 2527), focused Biome, touched-file typecheck scan with no diagnostics, clean touched-file `git diff --check`, clean `components/tables-2/core` diff, HTTP route smoke returning `200`, and follow-up authenticated browser proof confirming compact row/header sizing plus table-owned vertical/horizontal scroll after the local table blocker was resolved.
  - Previous slice: 2026-07-17 `/sales-book/shelf-items` density/width tuning now brings the Shelf Items table closer to Sales Orders/Midday invoices while preserving the existing shelf product/category management route, local filters, create/edit/toggle mutations, refresh behavior, and table-core contract. Product rows now use smaller thumbnails, tighter Product/Category/Price/Status/Actions widths (`200/360/240`, `160/280/200`, `92/132/104`, `128/220/150`, `104/132/116`), sticky Product/Actions columns, and `TABLE_CONFIGS["shelf-items"].rowHeight` set to `56`. Validation passed with focused shelf-items migration-parity tests (4 / 48), full `apps/www/src/components/tables-2` tests (305 / 2524), focused Biome, touched-file typecheck scan with no diagnostics, clean touched-file `git diff --check`, clean `components/tables-2/core` diff, browser proof for `/sales-book/shelf-items` proving `56px` rows plus table-owned vertical/horizontal overflow/scroll, header offset `0px -> 70px`, no document-level horizontal overflow, and screenshot evidence at `/private/tmp/gnd-shelf-items-table.jpg`.
  - Previous slice: 2026-07-17 `/sales-book/top-selling-products` and `/product-report` density/width tuning now brings the Sales Statistics/Product Report table closer to Sales Orders/Midday invoices while preserving the existing `sales.getProductReport` query, product-report filter params, `ProductReportHeader`, grid/table toggle, and product-detail row-open behavior. Both routes now use `ScrollableContent`; the table calls `useScrollHeader(parentRef)`, uses DnD/`DraggableHeader`, includes the header-offset spacer, and uses tighter Product/Category/Units/Revenue/Cost/Sales/Margin widths (`200/380/240`, `116/220/140`, `88/132/100`, `104/150/118`, `104/150/118`, `104/150/118`, `92/132/104`) with `TABLE_CONFIGS["sales-statistics"].rowHeight` set to `56`. Validation passed with focused Sales Statistics parity tests (4 / 45), full `apps/www/src/components/tables-2` tests (305 / 2523), focused Biome, touched-file typecheck scan with no diagnostics, clean touched-file `git diff --check`, clean `components/tables-2/core` diff, browser proof for `/sales-book/top-selling-products` and `/product-report` proving `56px` rows plus table-owned vertical/horizontal overflow/scroll, header offset `0px -> 70px`, no document-level horizontal overflow, and screenshot evidence at `/private/tmp/gnd-sales-statistics-table.jpg`.
  - Previous slice: 2026-07-17 `/sales-book/accounting/resolution-center` density/width tuning now brings the Sales Resolution Center table closer to Sales Orders/Midday invoices while preserving the existing resolution query/filter/header, row selection, detail-panel-outside-virtual-rows behavior, and sync-due action contracts. Order/Customer/Issue/Amounts/Due/Payments/Sales Rep/Actions widths are tightened to content-fit ranges (`132/220/154`, `180/340/220`, `140/240/170`, `124/180/144`, `124/180/144`, `104/150/118`, `96/150/112`, `124/170/136`), and `TABLE_CONFIGS["sales-resolution"].rowHeight` is now `56`. Validation passed with focused Sales Resolution parity tests (3 / 43), full `apps/www/src/components/tables-2` tests (301 / 2478), focused Biome, touched-file typecheck scan with no diagnostics, authenticated browser smoke proving `56px` rows plus table-owned vertical/horizontal overflow/scroll on `/sales-book/accounting/resolution-center`, header offset `0px -> 70px`, and screenshot evidence at `/private/tmp/gnd-sales-resolution-table.jpg`.
  - Previous slice: 2026-07-17 `/sales-book/accounting` density/width tuning now brings the Sales Accounting table closer to Sales Orders/Midday invoices while preserving the existing accounting query/filter/header, row selection/export, and `openSalesAccountingId` row-open URL state. Date/Amount/Description/Order #/Sales Rep/Processed By/Payment Status/Sub Total/Labor/Delivery/Actions widths are tightened to content-fit ranges (`118/180/136`, `104/150/118`, `180/360/220`, `140/260/170`, `112/190/136`, `112/180/130`, `130/220/150`, `104/150/118`, `92/132/104`, `92/132/104`, `64/64`), `TABLE_CONFIGS["sales-accounting"].rowHeight` is now `56`, and the table calls `useScrollHeader(parentRef)` plus uses DnD/draggable headers and a select-all header checkbox for Sales Orders-style table behavior. Validation passed with focused Sales Accounting parity tests (4 / 50), full `apps/www/src/components/tables-2` tests (301 / 2475), focused Biome, touched-file typecheck scan with no diagnostics, authenticated browser smoke proving `56px` rows plus table-owned vertical/horizontal overflow/scroll on `/sales-book/accounting?size=20`, header offset `0px -> 70px`, and screenshot evidence at `/private/tmp/gnd-sales-accounting-table.jpg`.
  - Latest cleanup slice: 2026-07-17 unused clean-code table engine removal deleted the isolated `table-cells`, `use-data-table`, `use-infinity-data-table`, `use-table-compose`, clean-code `data-table-column-header`, clean-code `query-options`, clean-code `filter-command/filters`, and `_v1/data-table` row-action/header helpers after precise import scans found no live callers. The still-live `search-params` and `Dl` utilities were preserved, and `page-sticky-header.test.ts` now allowlists only those two old clean-code utility files while asserting `_v1/data-table` has no source files. Follow-up guard hardening also asserts the old `components/tables` runtime tree has no source files, so reintroduced legacy table helpers fail the audit even if they do not import old primitives. Validation passed with the restarted page audit test (5 / 7 after the components/tables guard), deleted-module import scans finding only negative test assertions, a filtered `@gnd/www` typecheck scan returning no deleted-module diagnostics, focused Biome for the audit test, touched-file `git diff --check`, `find apps/www/src/components/tables -type f` returning no files, and a clean `components/tables-2/core` diff.
  - Previous cleanup slice: 2026-07-17 restarted table audit hardening added a live-source guard to `page-sticky-header.test.ts` that scans `apps/www/src/app`, `apps/www/src/components`, and `apps/www/src/features` for legacy/raw table systems outside `components/tables-2` and retired legacy internals. The guard blocks `@gnd/ui/data-table`, raw `@gnd/ui/table`, old `components/tables/*`, `_v1/data-table`, old clean-code `table-cells`, and raw JSX table tags. The remaining live sales overview `TCell.Date` usage was replaced with direct `formatDate` calls in the overview tab and legacy sheet. Validation passed with the restarted page audit test (4 / 4), static live-source table scans and `TCell` scans returning no output, focused Biome for the audit test plus `overview-tab.tsx`, and touched-file `git diff --check`; broad `@gnd/www` typecheck still reports pre-existing Sales Overview type debt.
  - Previous slice: 2026-07-17 `/sales-book/inbound-management` density/width tuning now brings the Inbound Management table closer to Sales Orders/Midday invoices while preserving the existing inbound query/filter/header, packing-list action, and row-open URL state. Order/Customer/Sales Rep/Status/Actions widths are tightened to content-fit ranges (`132/220/154`, `180/340/220`, `86/140/104`, `116/180/132`, `64/64`), `TABLE_CONFIGS["inbound-management"].rowHeight` is now `56`, and the table calls `useScrollHeader(parentRef)` plus uses DnD/draggable headers for Sales Orders-style table behavior. Validation passed with focused Inbound Management parity tests (4 / 43), full `apps/www/src/components/tables-2` tests (297 / 2425), focused Biome, touched-file typecheck scan with no diagnostics, authenticated browser smoke proving `56px` rows plus table-owned vertical overflow/scroll on `/sales-book/inbound-management?size=20`, header offset to `70px`, desktop content-fit width with no unnecessary horizontal overflow, and screenshot evidence at `/private/tmp/gnd-inbound-management-table.png`.
  - Previous slice: 2026-07-17 `/sales-book/dispatch` density/width tuning now brings the Dispatch table closer to Sales Orders/Midday invoices while preserving the schedule picker, two-line Ship To/progress content, and menu controls. Schedule/Order/Order Date/Ship To/Assigned To/Progress/Status/Actions widths are tightened to content-fit ranges (`118/180/136`, `140/230/160`, `104/150/118`, `180/360/220`, `132/220/160`, `118/180/132`, `116/170/132`, `72/72`), `TABLE_CONFIGS["sales-dispatch"].rowHeight` is now `56`, and the table calls `useScrollHeader(parentRef)` for Sales Orders-style header offset on table scroll. Validation passed with focused Dispatch parity tests (4 / 39), full `apps/www/src/components/tables-2` tests (293 / 2382), focused Biome, touched-file typecheck scan with no diagnostics, authenticated browser smoke proving `56px` rows plus table-owned vertical/horizontal overflow/scroll on `/sales-book/dispatch?size=20`, header offset `0px -> 70px`, and screenshot evidence at `/private/tmp/gnd-sales-dispatch-table.png`.
  - Previous slice: 2026-07-17 `/sales-book/quotes` density/width tuning now brings the Quotes table closer to Sales Orders/Midday invoices: Customer/Phone/Address/Invoice/Status/Sales rep widths are tightened to content-fit ranges while `TABLE_CONFIGS["sales-quotes"].rowHeight` remains the canonical compact `40`; the Actions column intentionally stays `144px` because it contains four compact icon controls. Validation passed with focused Sales Quotes plus Sales Rep embed parity tests (8 / 97), full `apps/www/src/components/tables-2` tests (289 / 2343), focused Biome, touched-file typecheck scan with no diagnostics, `git diff --check`, authenticated browser smoke proving `40px` rows plus table-owned vertical/horizontal overflow/scroll on `/sales-book/quotes`, and screenshot evidence at `/private/tmp/gnd-sales-quotes-table.png`.
  - Previous slice: 2026-07-17 `/sales-book/dealers` density/width tuning now brings the Dealers table closer to Sales Orders/Midday invoices: the dealer cell is single-line with a smaller `size-6` identity marker, the sales profile selector is an `h-8` compact trigger with inline coefficient text, Dealer/Email/Status/Sales Profile/Customer Link/Created/Actions widths are tightened to content-fit ranges, and `TABLE_CONFIGS["dealers"].rowHeight` is `48` with compact padding. Validation passed with focused Dealers parity tests (5 / 43), full `apps/www/src/components/tables-2` tests (289 / 2340), focused Biome, touched-file typecheck scan with no diagnostics, `git diff --check`, and authenticated browser smoke proving `48px` rows plus table-owned horizontal overflow/scroll on `/sales-book/dealers`; the local fixture has only 5 dealer rows, so natural vertical overflow was not available for this slice.
  - Previous slice: 2026-07-17 `/sales-book/customers` density/width tuning now brings the Customers table closer to Sales Orders/Midday invoices: the customer cell is single-line with a smaller `size-6` identity marker, Customer/Phone/Secondary/Email/Address/Actions widths are tightened to content-fit ranges, and `TABLE_CONFIGS["customers"].rowHeight` is `48` with compact padding. Validation passed with focused Customers parity tests (4 / 36), full `apps/www/src/components/tables-2` tests (289 / 2338), focused Biome, touched-file typecheck scan with no diagnostics, `git diff --check`, and authenticated browser smoke proving `48px` rows, table-owned vertical overflow/scroll, and narrow-viewport horizontal overflow/scroll on `/sales-book/customers`.
  - Previous slice: 2026-07-17 legacy Sales Form take-off HPT now uses `tables-2/sales-form-takeoff-hpt-lines` inside `components/forms/sales-form/take-off/hpt-form.tsx`, replacing the embedded raw one-row `<table>` while preserving HPT provider bindings, dev item UID display, size selector, swing selector, handle/no-handle quantity inputs, price estimate menu, labor-per-quantity popover, and animated line total. The new embedded table registers compact 52px rows, a sticky Size column, content-fit Size/Swing/Qty/LH/RH/Estimate/Labor/Total widths, forced Swing and Qty-vs-LH/RH visibility, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings; the old header-only `PROD` column was not carried forward because it had no matching body cell or control. Validation passed with full `apps/www/src/components/tables-2` tests (289 / 2337), focused take-off HPT parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics, raw-table static scan on the take-off form, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 clean-code Sales Form door-size select modal now uses `tables-2/clean-code-door-size-select-lines` inside `app-deps/(clean-code)/(sales)/sales-book/(form)/_components/modals/door-size-select-modal`, replacing the embedded raw `@gnd/ui/table` / `ScrollArea` grid while preserving the clean-code `DoorSizeSelectProvider` context, supplier badge, modal footer actions, swap-door path, Super Admin price edit popover, non-admin price display, swing select, and handle/no-handle quantity field names. The new embedded table registers compact 52px rows, a sticky Size column, content-fit Size/Price/Swing/Qty/LH/RH widths, forced mode visibility for Swing and Qty-vs-LH/RH, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (285 / 2337), focused clean-code door-size select parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics, raw-table static scan across the clean-code sales-form `_components` audit path, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy modal slice.
  - Previous slice: 2026-07-17 clean-code Sales Form moulding step now uses `tables-2/clean-code-sales-form-moulding-lines` inside `app-deps/(clean-code)/(sales)/sales-book/(form)/_components/moulding-step`, replacing the desktop raw `@gnd/ui/table` / `table-fixed` markup while preserving the clean-code `MouldingClass` context, dynamic item-type header label, selected-line filtering, moulding title, quantity calculator, quantity input, price summary menu, custom price input, addon input, animated line total, and delete confirmation. The new embedded table registers compact 48px rows, a sticky Moulding column, content-fit S/N/Moulding/Qty/Estimate/Addon/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (281 / 2337), focused clean-code moulding-line parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics, raw-table static scan on the clean-code moulding step, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 clean-code Sales Form service step now uses `tables-2/clean-code-sales-form-service-lines` inside `app-deps/(clean-code)/(sales)/sales-book/(form)/_components/service-step`, replacing the desktop raw `@gnd/ui/table` / `table-fixed` markup while preserving the mobile card path, clean-code `ServiceClass` context, service description input, tax switch, production switch, quantity input, price input, animated line total, delete confirmation, and Add Line action. The new embedded table filters selected service rows into stable table-core rows and registers compact 48px rows, a sticky Description column, content-fit S/N/Description/Tax/Prod/Qty/Price/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (277 / 2337), focused clean-code service-line parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics, raw-table static scan on the clean-code service step, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 legacy Sales Form shelf items now use `tables-2/sales-form-shelf-items` inside the existing Shelf Items sales form section, replacing the nested desktop raw `@gnd/ui/table` / `table-sm` markup while preserving the mobile card path, shelf category combobox, category create/clear behavior, product combobox, product add/delete behavior, price popover, quantity input, animated totals, and existing `kvFormItem.<item>.shelfItems` form-store contract. The new embedded table flattens section/product lines into table-core rows, keeps category editing on the first section row, adds an `Add Product` row per section, and registers compact 56px rows, a sticky Category column, content-fit S/N/Category/Product/Price/Qty/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (273 / 2337), focused sales-form shelf-items parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics while broad `@gnd/www` remains blocked by unrelated baseline API/UI errors, raw-table static scans across `components/forms/sales-form`, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 legacy Sales Form HPT lines now use `tables-2/sales-form-hpt-lines` inside the existing HPT sales form section, replacing the desktop raw `@gnd/ui/table` / `table-sm` markup while preserving the mobile card path, HPT line context bindings, size display, production override switch, swing input, handle/no-handle quantity modes, price estimate, animated line total, note toggle/panel behavior, delete confirmation, and Add Door Size control. The new embedded table registers compact 48px rows, a sticky Size column, content-fit S/N/Size/Prod/Swing/Qty/Lh/Rh/Estimate/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, persisted settings, and forced visibility for slab/swing/no-handle modes. Validation passed with full `apps/www/src/components/tables-2` tests (269 / 2337), focused sales-form HPT-line parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics while broad `@gnd/www` remains blocked by unrelated baseline API/UI errors, raw-table static scans, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 legacy Sales Form moulding lines now use `tables-2/sales-form-moulding-lines` inside the existing Moulding/Service sales form section, replacing the desktop raw `@gnd/ui/table` / `table-sm` markup while preserving the mobile card path, line item provider bindings, moulding description display, quantity input, price summary menu, addon input, custom price input, animated total, delete confirmation, and Add Moulding popover. The new embedded table registers compact 48px rows, a sticky Moulding column, content-fit S/N/Moulding/Qty/Estimate/Addon/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (265 / 2337), focused sales-form moulding-line parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics while broad `@gnd/www` remains blocked by unrelated baseline API/UI errors, raw-table static scans, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 legacy Sales Form service lines now use `tables-2/sales-form-service-lines` inside the existing Moulding/Service sales form section, replacing the desktop raw `@gnd/ui/table` / `table-sm` markup while preserving the mobile card path, line item provider bindings, service input, tax switch, production switch behavior, quantity input, price input, animated total, delete confirmation, and `valueChanged` wiring where the old desktop table used it. The new embedded table registers compact 48px rows, a sticky Service column, content-fit S/N/Service/Tax/Production/Qty/Price/Total/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (261 / 2337), focused sales-form service-line parity tests (4), focused Biome, touched-file typecheck scan with no diagnostics while broad `@gnd/www` remains blocked by unrelated baseline API/UI errors, raw-table static scans, and `git diff --check`. Browser DOM proof was not run for this embedded legacy form slice.
  - Previous slice: 2026-07-17 Employee form permissions grid now uses `tables-2/employee-form-permissions` inside the existing employee create/edit modal, replacing embedded `@gnd/ui/table` permission markup while preserving employee defaults, role/profile/office fields, save/invalidation behavior, and the selected permission id array contract. The visible `Create` column toggles each permission `viewPermissionId`; the visible `Edit` column toggles `editPermissionId`. The new embedded table registers compact 48px rows, a sticky Permission column, content-fit Permission/Create/Edit widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (249 / 2337), focused employee form permissions plus role/employees parity tests (14 / 44), focused Biome, touched-file typecheck scan, `/hrm/employees/v2` HTTPS proxy smoke returning `200`, legacy inline-table scan, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded modal form slice.
  - Previous slice: 2026-07-17 Role form permissions grid now uses `tables-2/role-form-permissions` inside the existing roles/profile sheet role form, replacing embedded `@gnd/ui/table` permission markup while preserving role name, form reset, submit/cancel behavior, loading skeletons, `createRoleAction` payload shape, and the existing visible `Create` -> `permissions.view <permission>.checked` plus `Edit` -> `permissions.edit <permission>.checked` field mapping. The new embedded table registers compact 48px rows, a sticky Permission column, content-fit Permission/Create/Edit widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (245 / 2337), focused role permissions plus roles/profile sheet parity tests (13 / 80), focused Biome, touched-file typecheck scan, `/hrm/employees/v2` HTTPS proxy smoke returning `200`, legacy inline-table scan, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded sheet form slice.
  - Previous slice: 2026-07-17 Inventory Product form sub-components grid now uses `tables-2/inventory-product-form-sub-components` inside the existing inventory product form, replacing embedded `@gnd/ui/table` / `table-sm` markup and the incomplete row-sortable wrapper while preserving add-row behavior, component-category selection, default product selection, sub-component status mutation, delete confirmation, and existing inventory sub-component mutation contracts. The new embedded table registers compact 52px rows, sticky Handle and Category columns, a sticky right Actions column, content-fit Handle/Category/Default Product/Status/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, horizontal pagination, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (241 / 2337), focused Inventory Product sub-components plus variant-grid parity tests (8), focused Biome, touched-file typecheck scan, `/inventory` direct Next and HTTPS proxy smokes returning `200`, legacy inline-table scan, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this embedded form slice.
  - Previous slice: 2026-07-17 Inventory Product form variant grid now uses `tables-2/inventory-product-form-variants` inside the existing inventory product form, replacing embedded `@gnd/ui/table` markup, manual page/window sticky header state, `IntersectionObserver`, and Show More pagination while preserving variant filters, selected variant URL params, status mutation behavior, stock-monitor conditional columns, and the selected variant pricing panel. The selected detail panel now renders below the fixed-height virtual table, and the new embedded table registers compact 52px rows, a sticky Variant column, a sticky right Actions column, content-fit Variant/Cost/Stock/Low/Status/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, horizontal pagination, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (237 / 2337), focused Inventory Product form variants plus inventory products/variants parity tests (7 / 16), focused Biome, touched-file typecheck scan, `/inventory` direct Next and HTTPS proxy smokes returning `200`, legacy table/window-scroll/static pagination scan, `git diff --check`, and a clean `components/tables-2/core` diff. Browser DOM proof was not run for this slice.
  - Previous slice: 2026-07-17 Builder form task grid now uses `tables-2/builder-form-tasks` inside the existing Community Builders modal, replacing the embedded `@gnd/ui/namespace` table markup while preserving the builder fields, form reset, legacy upgrade notice, save integration, and task add/remove behavior. The address field now uses the existing textarea-capable `FormInput`, and the legacy upgrade action avoids a non-null builder id assertion. The new embedded table registers compact 48px rows, a sticky Task column, content-fit Task/Addon/Billable/Job/Productionable/Actions widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (233 / 2337), focused Builder form task-grid parity tests (4), combined Builder form and Community Builders parity tests (8 / 37), focused Biome, touched-file typecheck scan, `/community/builders` direct Next and HTTPS proxy smokes returning `200`, legacy table markup scan, `git diff --check`, and a clean `components/tables-2/core` diff. Playwright CLI screenshot in an unauthenticated context was blank and was not counted as browser proof.
  - Previous slice: 2026-07-17 Community Install Cost form task grid now uses `tables-2/community-install-cost-form-tasks` inside the existing Community Template install-cost modal, replacing the desktop embedded `@gnd/ui/table` / `table-sm` markup while preserving form reset, save mutation, modal/sidebar footer placement, and install-cost meta payload behavior. The old blank `Def. Qty` cells now render task `defaultQty` when present, while editable quantities still write `installCost.<uid>`. The new embedded table registers compact 48px rows, a sticky Task column, content-fit Task/Def. Qty/Qty widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, active quantity row highlighting, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (229 / 2337), focused Community Install Cost form task-grid parity tests (4), combined community install-cost/model-cost parity tests (13 / 44), focused Biome, touched-file typecheck scan, `/community/templates` direct Next and HTTPS proxy smokes returning `200`, legacy table markup scan, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Community Model Cost form task grid now uses `tables-2/community-model-cost-form-tasks` inside the existing Community Template model-cost modal, replacing the desktop embedded `@gnd/ui/table` / `table-sm` markup while preserving start/end dates, Clear Costs, Create Copy, Delete, totals, save/delete mutations, and Unit Invoice page-tab invalidation. The new embedded table registers compact 48px rows, a sticky Task column, content-fit Task/Cost/Tax widths, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (225 / 2337), focused Community Model Cost form task-grid parity tests (4), focused Biome, touched-file typecheck scan, `/community/templates` direct Next smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Unit Invoice edit-form task grid now uses `tables-2/unit-invoice-form-tasks` inside the existing edit modal, replacing the desktop embedded `@gnd/ui/table`/`table-sm` markup while preserving the mobile card editor, task add/delete/save flow, locked generated-task fields, first-check sync controls, and Unit Invoice page-tab invalidation. The new embedded table registers compact 52px rows, a sticky Task column, table-owned scroll, virtual rows, DnD, draggable headers, resize handles, column dividers, and persisted settings. Validation passed with full `apps/www/src/components/tables-2` tests (221 / 2327), focused Unit Invoices/page-sticky tests (9 / 43), focused Biome, touched-file typecheck scan, `/community/unit-invoices` route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 legacy runtime cleanup removed the remaining unused `apps/www/src/components/tables` files after exact scans confirmed no live `apps/www` consumers outside negative audit assertions. The folder now has no files, live non-test import scans for `components/tables` in `apps/www/src`, `packages`, and `apps/api/src` are clean, the restarted-page audit passed, the full `apps/www/src/components/tables-2` suite passed with 220 tests / 2314 assertions, and `components/tables-2/core` stayed unchanged.
  - Previous slice: 2026-07-17 legacy table cleanup removed unused old `components/tables/community-project`, `components/tables/project-units`, `components/tables/unit-invoices`, `components/tables/unit-productions`, `components/tables/contractor-jobs`, and `components/tables/contractor-payouts` files after their routes and Project Overview embeds had already moved to `tables-2`. Static import scans found no live source consumers outside Brain history and negative audit assertions, and `components/tables-2/core` stayed unchanged.
  - Previous slice: 2026-07-17 Inventory Item Dashboard now uses `tables-2/inventory-item-dashboard` on `/inventory/[id]`, replacing the hand-rendered variants, stock, movement history, inbound demand, allocations, and related sales/quotes sections with compact table-core sections. The slice keeps `inventories.inventoryItemDashboard`, not-found fallback, item metrics, top-sales analytics, and operational links, registers compact section table settings with tailored content-fit widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Inventory Item Dashboard migration parity tests (4 / 65), full restarted table suite (220 / 2314), targeted Biome, touched-file typecheck log scan, static scans for old dashboard section helpers/manual fetch/old table patterns, HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-22 Sales Book Inbounds now uses the standard `SearchFilterTRPC` input with URL-backed `q`, `status`, and `inboundId` state; sidebar links and inventory-owned inbound-management actions target `/sales-book/inbounds` with direct shipment selection when available, while the legacy management route remains available. Focused parity/sidebar tests passed (13 / 83), targeted Biome, WWW typecheck, and `git diff --check` passed. Authenticated browser proof for deep links, back/forward, and retargeted actions remains open.
  - Previous slice: 2026-07-17 Bug Report Access Employees now uses `tables-2/bug-report-access-employees` on `/settings/bug-reports`, replacing hand-mapped employee access rows and inline switches with a compact table-core settings table. The slice keeps the guarded client `hrm.getEmployees` query, Super Admin-only route, enabled counts, role-enabled disabled state, and `hrm.setEmployeeBugReportingAccess` mutation behavior, registers compact 56px rows with tailored Employee/Role/Account/Status/Access widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Bug Reports migration parity tests (9 / 147), full restarted table suite (213 / 2209), targeted Biome, touched-file typecheck log scan, static scans for hand-mapped employee row/inline switch patterns, HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Contractor Payout Detail Included Jobs now uses `tables-2/contractor-payout-overview-jobs` and the restarted route shell on `/contractors/jobs/payments/[paymentId]`, replacing hand-mapped included-job cards and manual `getQueryClient().fetchQuery` route hydration with a compact embedded table. The slice keeps the existing `jobs.contractorPayoutOverview` query, payout cancel/reverse actions, print report action, summary cards, adjustments, and activity history, registers compact 64px rows with tailored Job/Location/Status/Amount/Created widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Contractor Payouts parity tests (6 / 70), full restarted table parity suite (212 / 2169), targeted Biome, touched-file typecheck log scan, static scans for included-job card/manual fetch/old table patterns, HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Customer Overview Sales Preview now uses `tables-2/customer-overview-sales-preview` and the restarted route shell on `/sales-book/customers/v2/[accountNo]`, replacing raw sales/quotes preview tables and manual `getQueryClient().fetchQuery` route hydration with a compact embedded preview table. The slice keeps the existing `customers.getCustomerOverviewV2` query, quote/order sheet open, quote/order page open, payment/reminder actions, and sheet-mounted customer overview content, registers compact 56px rows with tailored Reference/Date/Amount/Status/Actions widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Customer Overview Sales Preview parity tests (5 / 52), full restarted table parity suite (209 / 2115), targeted Biome, touched-file typecheck log scan, static scans for raw preview table/manual fetch/old table patterns, HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Notification Channels now uses `tables-2/notification-channels` and the restarted route shell on `/settings/notification-channels/v2`, replacing the card-mapped channel selector and manual `getQueryClient().fetchQuery` route path with a compact selectable channel table. The slice keeps the existing `notes.getNotificationChannels`, `notes.getNotificationChannel`, delivery toggle, role/subscriber mutation, sync retry, and `openNotificationChannelId` behavior, registers compact 56px rows with tailored Channel/Priority/Delivery/Audience/Category/Status/Actions widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Notification Channels parity tests (3 / 33), full restarted table parity suite (195 / 1941), targeted Biome, touched-file typecheck log scan, static scans for card-mapped channel rows/manual fetch/old table patterns, HTTP/HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff. Follow-up authenticated browser proof closed the scroll gap with `57` rows, `56px` row heights, a `45px` header, table-owned vertical/horizontal scroll, and no document-level horizontal overflow.
  - Previous slice: 2026-07-17 Bug Reports now uses `tables-2/bug-reports` and the restarted route shell on `/support/bug-reports`, replacing card-mapped report buttons with a compact selectable report table. The slice keeps the existing `bugReports.mine`, `bugReports.adminList`, `bugReports.byId`, status update, screenshot/video evidence detail, voice-note follow-up rendering, and add-follow-up behavior, registers compact 64px rows with tailored Report/Status/Capture/Replies/Submitted/Actions widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Bug Reports parity tests (3 / 40), full restarted table parity suite (192 / 1908), targeted Biome, touched-file typecheck log scan, static scans for card-mapped report rows/manual fetch/old table patterns, HTTP/HTTPS route smoke returning `200`, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Document Approvals now uses `tables-2/document-approvals` and the restarted route shell on `/hrm/document-approvals`, replacing card-mapped approval rows with a compact approval queue table. The slice keeps the existing `getEmployeeDocumentApprovals` and `reviewEmployeeDocument` server actions, Open Review URL-param behavior, success toasts, route refresh, and document URL opening, now registers compact 56px rows with tightened Employee/Document/Status/Dates/Reviewed/Actions widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Document Approvals parity tests, targeted Biome, touched-file typecheck log scan, static scans for card-mapped approval rows/manual fetch/old table patterns, `git diff --check`, and a clean `components/tables-2/core` diff. The original runtime smoke was environment-limited, and the later authenticated browser follow-up closed the row/header/scroll proof gap.
  - Previous slice: 2026-07-17 Task Events now uses `tables-2/task-events` and the restarted route shell on `/task-events`, replacing the manual `getQueryClient().fetchQuery` route path and card-mapped event list in the dashboard. The slice keeps the existing `taskEvents.list` contract, client search, refresh action, Task Diagnostics link, and event-open behavior, registers compact 64px rows with tailored Event/Status/Last Run/Records/Latest Result/Actions widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Task Events parity tests (3 / 37), full restarted table parity suite (186 / 1830), targeted Biome, touched-file typecheck log scan, static scans for card-mapped list/raw table/manual fetch patterns, `git diff --check`, and a clean `components/tables-2/core` diff. Runtime route smoke timed out on both the local HTTPS proxy and direct HTTP dev-server URL after 20s with no bytes.
  - Previous slice: 2026-07-17 Task Event History now uses `tables-2/task-event-history` and the restarted route shell on `/task-events/[eventName]`, replacing the manual `getQueryClient().fetchQuery` route path and raw run-history `<table>` in the detail component. The slice keeps the existing task-event get/history/update/runNow/runTest contracts, status save, run controls, sales-order filter editor, manual payment-report run flow, history refetch, and task-event list invalidation behavior, registers compact metadata rows with tailored Time/Value/Trigger/Meta widths, and keeps `components/tables-2/core` unchanged. Validation passed with focused Task Event History parity tests (3 / 36), full restarted table parity suite (183 / 1793), targeted Biome, touched-file typecheck log scan, static scans for raw table/manual fetch patterns, `git diff --check`, and a clean `components/tables-2/core` diff. Runtime route smoke timed out on both the local HTTPS proxy and direct HTTP dev-server URL after 20s with no bytes.
  - Previous slice: 2026-07-17 Inventory Suppliers now uses `tables-2/inventory-suppliers` and the restarted route shell on `/inventory/suppliers`, replacing the manual `getQueryClient().fetchQuery` route path and old compact item-list rows in the supplier manager. The slice keeps the existing inventory supplier query/mutation/sync contracts, preserves search/create/edit/delete/default/sync/Dyke-review behavior, registers compact 56px rows with tailored Supplier/Contact/Address/Actions widths, and invalidates supplier directory/review queries after mutations. Validation passed with focused Inventory Suppliers parity tests (5 / 43), full restarted table parity suite (180 / 1757), targeted Biome, touched-file typecheck log scan, static scans for old supplier item-list/manual fetch patterns, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Community Install Costs now uses `tables-2/community-install-costs` and the restarted route shell on `/community/install-costs`, replacing the old namespace table/manual `fetchQuery` route path and deleting `InstallCostLine`. The slice keeps the existing install-cost query/mutation/import contracts, preserves inline create/edit/save plus legacy import fallback, registers compact 48px rows with tailored Task/Cost/Unit/Actions widths, and relabels the old misleading `Max Qty` column to `Unit`. Validation passed with focused Community Install Costs parity tests (5 / 44), full restarted table parity suite (175 / 1714), targeted Biome, touched-file typecheck log scan, runtime static scans for old install-cost table/manual fetch patterns, `git diff --check`, and a clean `components/tables-2/core` diff.
  - Previous slice: 2026-07-17 Transaction Overview modal now uses `tables-2/transaction-overview-applications` and `tables-2/transaction-overview-payments` instead of inline `@gnd/ui/table`, manual skeleton rows, and old `TCell` helpers. The modal hydrates both table settings from the clean-code layout, keeps the transaction summary and sales-overview open behavior, uses compact 40px application rows and 48px payment rows, sticky Invoice/Date columns, table-owned scroll, DnD, resize, persisted settings, and tight modal-fit Invoice/Applied/Status plus Date/Description/Status/Actions widths. Validation passed with focused Transaction Overview parity tests (3 / 48), full restarted table parity suite (169 / 1666), targeted Biome, touched-file typecheck grep, JSX-aware static scan, `git diff --check`, and a clean `components/tables-2/core` diff; runtime smoke was not run because opening this modal requires an authenticated transaction row selection.
- [x] 2026-06-09 sales quote PDF download regression: implementation and release proof are complete. HTML preview downloads use the same-origin blob path with credentials/no-store and preserve canonical quote mode; focused service/print/PDF coverage is green (42 tests / 154 assertions). Authenticated browser proof on 2026-07-23 rendered quote `03341LM`, returned HTTP 200 from the quote-mode download endpoint, and persisted a valid one-page PDF (Print + UI + Validation)
- [x] 2026-06-09 copy quote/sales visibility regression: copied quote/sales records did not show up until browser refresh; fixed by centralizing web sales document/payment query invalidation across create/update, copy/move/delete, dashboard/list/filter/overview, and transaction/accounting payment reads (UI + Validation)
- [x] **Dead Code & Dead Link Cleanup 2026-04-10** — deleted 180 knip-identified unused files from `apps/www`; full dead-link analysis documented at `brain/decisions/dead-code-dead-links-cleanup-2026-04-10.md`; **action needed**: 7 broken nav links in `sidebar/links.ts` (dealers URL wrong, typo in quotes, missing commissions/pickups pages)
- [x] Sales PDF V2: isolated multi-template print system (`brain/features/sales-pdf-system.md`) — COMPLETED: Phase 4 (salesV2 tRPC endpoint), Phase 6 (print-sales-v2.tsx + /p/sales-invoice-v2 route), quick-print helper, sales-menu-print component
- [ ] Employee Management V2 — remaining phases: DB schema migration (`EmployeeRecord`), insurance gate on job creation, expo app mirror; shipped slices include `[employeeId]` overview route, `employees.route.ts` API layer, employee-specific permission overrides/counts, a jobs-dashboard insurance-gate freshness fix so submit CTAs wait for a fresh profile read instead of treating loading state as missing insurance, a dev-only web debug pill showing contractor custom-job activation beside the Tailwind breakpoint indicator, and a shared web/Expo job-save source stamp with the website job form now requiring an existing project/unit before save
- [x] Sales PDF V2 follow-up slice: implementation and release proof are complete—sales-form and Sales Overview quick print use the shared V2 controller, previews use the HTML template renderer, access resolution deduplicates concurrent requests, per-order stored print data is reused, and batch requests preserve order while merging cached/generated pages. Authenticated 2026-07-23 proof persisted a valid quote PDF and a visually inspected two-page merged artifact ordered `08894-LM`, then `08893-LM` (`brain/features/sales-pdf-system.md`)
- [ ] Sales form system hardening Phase 0: implementation complete—transactional save safety and server-authoritative pricing validation are covered by the API save path and relational parity fixtures (29 tests / 237 assertions, plus 3 post-save timeout tests). Remaining gate is authenticated browser/runtime proof against the real database (`brain/sales-form-system-hardening-plan.md`) (Schema + API + Validation)
- [ ] Sales form system hardening Phase 1: partial implementation complete—labor writeback no longer blocks pricing persistence and legacy subtotal surfaces use `pricing.subTotal`, with focused coverage. Remaining gate is full pricing permutation/browser proof for taxable consistency and labor/flat-labor interactions (API + UI + Validation)
- [ ] New sales form resilience hardening: implementation complete—autosave now defaults on, dirty payloads persist to versioned local recovery snapshots on change/page-leave, and risky navigation warns when autosave is off/stale/errored. Remaining gate is authenticated browser proof of save, restore, and leave-warning behavior (UI + Validation)
- [ ] Legacy sales-form mobile + architecture refactor: create `domains/sales-form/legacy`, centralize legacy classes/helpers behind adapters, flatten the shell UI, switch mobile to active-item editing with wrapped step CTA navigation, make HPT/moulding/service/shelf-items mobile-native, and extract save orchestration into application/server boundaries (`brain/legacy-sales-form-mobile-architecture-plan.md`) (Architecture + UI + API + Validation)
- [ ] Inventory-backed sales fulfillment master model: core projection, allocation review, inbound demand/receiving, production planning, backorder queue, ship-available flow, sync monitor, backfill jobs, and inventory-print foundation are documented in `brain/features/inventory-backed-sales-fulfillment.md`; next slices are inventory-to-Dyke create/update/delete sync, inventory variant price sync back to Dyke, production assignment/completion inventory lifecycle bridge, inventory-mode dispatch assign/pack/fulfill, print parity completion, item dashboard/variant analytics, reconciliation jobs, browser validation, and safer repeat-receive/allocation guards (Schema + Architecture + Inventory + Fulfillment + Print + Validation)
- [ ] Dyke-to-inventory domain migration: moved the active custom-component write path off `apps/www` server actions and into `@gnd/inventory` services exposed via inventories tRPC (`saveDykeStepComponent`, `updateDykeComponentPricing`), added async `sync-dyke-step-to-inventory` job wiring plus drift/repair endpoints, and switched the legacy sales-form custom component UI to call the new domain; next slice is migrating the remaining dormant Dyke admin/use-case helpers and deciding whether Dyke pricing should structurally sync into inventory pricing or remain sales-only (Architecture + API + Inventory)
- [ ] Supplier pricing integration for doors: introduced `Supplier` / `SupplierVariant` direction in inventory schema, added Dyke-supplier sync bridge by legacy UID, exposed supplier/supplier-variant mutations through inventories tRPC, added supplier management plus variant supplier-pricing editing inside the inventory form, upgraded the dedicated `/inventory/suppliers` workspace to the restarted `tables-2/inventory-suppliers` table with search/create/edit/delete/default actions, and wired sales door pricing to prefer exact `SupplierVariant` matches while preserving legacy supplier dep-key buckets as fallback; next slice is backfilling safe door supplier pricing data into `SupplierVariant`, browser-validating the new suppliers workspace + default-supplier flow, and tightening preferred-supplier use in inbound/purchasing (Schema + API + UI + Inventory)
- [ ] Door supplier pricing canonicalization: main/full inventory import now auto-syncs Dyke suppliers before importing, both import strategies now parse supplier-specific Dyke pricing keys and upsert canonical `SupplierVariant` rows instead of keeping those prices only in legacy pricing buckets, the sales pricing resolver now expects supplier-specific prices to come from `SupplierVariant`, and the inventory supplier form now shows a Dyke-supplier matching review so UID mismatches are visible; next slice is browser validation plus a real-data import rerun to confirm supplier rows and unmatched review counts look correct (Import + Pricing + UI)
- [ ] Inventory vs component split: added `Inventory.productKind`, inventory/components list filtering, inventory-form type toggle, component-mode form hiding for variants/pricing/stock sections, importer classification defaults, a priced-item backfill mutation that marks anything with meaningful pricing as `inventory` and leaves unpriced Dyke/config rows as `component`, tightened storefront/receiving queries to exclude `component` rows by default, added a dedicated `/inventory/review` admin mismatch report, and simplified the inventory workspace shell so analytics live on `/inventory` only while subpages no longer inherit the old shared tab/dashboard layout; next slice is refining heuristics for edge cases and deciding whether any stockable-but-unpriced records should be force-kept as `inventory` via source-aware rules beyond pricing alone (Schema + API + UI + Import)
- [ ] Category-kind split: `InventoryCategory` now carries its own `productKind`, product forms request category lists by kind, sub-component selectors use component categories, sub-category selectors use inventory categories, and the categories workspace can switch between inventory and component categories without mixing the two sources; next slice is backfilling existing categories safely and reviewing whether any legacy mixed-use categories need manual reassignment before stricter cleanup rules land (Schema + API + UI)
- [ ] Inventory import control center: replaced the imports page’s table-first/per-category workflow with a global control-center direction that surfaces import analytics, system checks, full update/check/reset actions, and leaves the legacy category table as a secondary read-only diagnostic; import scope is now driven by the active sales-settings route graph plus dependency closure instead of all Dyke steps, targeted Dyke syncs now skip out-of-scope steps, the optimized importer now dedupes category-variant attributes, subcategory links, and image links against both existing DB state and in-flight rows, and the imports UI reports active/dependency/excluded/stale counts; full import actions now queue Trigger jobs instead of blocking synchronously, persist authenticated operator/scope/strategy/terminal evidence through bounded `TaskRunDiagnostic` history, and refresh after completion; source-review rows now support audited single/batch same-kind retain/move disposition, projection queue failures have actor-attributed one-at-a-time retry evidence plus health badges/checks, and stale category cleanup is dry-run-first behind a transaction-guarded zero-live-child gate. Remaining work is production operator evidence and any failures discovered from that evidence (UI + Import + Operations)
- [x] Inventory import source labeling: imported Dyke rows preserve `sourceStepUid`, `sourceComponentUid`, and `sourceCustom`; default inventory, storefront, inbound-matching, and low-stock reads exclude custom imports; the control center exposes standard/custom counts, guarded stale-source review/archive, explicit single/batch retained disposition, stale-category cleanup, and durable projection failure/retry evidence (Schema + API + UI + Import)
  - 2026-07-23 slice: protected source review now classifies excluded/orphaned/incomplete imported rows as archive candidates, custom review, or operationally protected and links bounded candidates from the import control center; a confirmation-gated, dry-run-by-default archive mutation now revalidates and soft-archives only unused standard candidates, while custom/protected/stale rows remain skipped evidence. Authenticated control-center and cleanup-gate browser proof is complete; archive/disposition interaction proof still needs a live stale-source fixture.
  - 2026-07-23 retained-disposition slice: any reviewed row can be moved to an active same-kind category as standard operational inventory or an explicit custom exception; the mutation exact-baseline guards, detaches legacy source UIDs, transactionally records actor/before/after `Event` evidence, and exposes projection queue outcome.
  - 2026-07-23 batch/projection slice: the progressive batch control moves up to 12 visible same-kind rows through a 25-row unique server cap while preserving independent row baselines/audits; each projection attempt is durable, and a failed diagnostic can be claimed and retried once with new actor/run evidence.
- [ ] Inventory app-domain split: moved the inventory App Router tree out of `apps/www/src/app/(sidebar)/(sales)/inventory` and into `apps/www/src/app/(sidebar)/inventory` so inventory now lives as its own workspace/domain in the web app while preserving the same `/inventory` URLs; next slice is removing any remaining “sales-owned” assumptions in shared labels, filters, and supporting modules where inventory still reads as a sales subfeature (Architecture + UI)
- [ ] New sales form parity hotfix/repro batch: grouped component edit/image/redirect, door pricing, HPT estimate/add-size, and moulding outside-click paths are implemented in code; the HPT `Add Door` action now returns to Door multi-select without changing configured rows. Remaining work is authenticated runtime proof for the parity matrix and any fixture-specific discrepancies (API + UI + Validation)
- [ ] Sales Orders V2: build the clean `sales-book` orders workspace on dedicated v2 query/filter contracts, summary widgets, reusable invoice-style table shell, and unified funnel status system (`brain/features/sales-orders-v2.md`)
- [ ] Pickup packing funnel: `Send for Pickup` now routes packing work into queued pickup deliveries with `sales-packing-list` notification membership, the canonical warehouse page lives at `/sales/packing-list` under the sidebar tree with `Current` / `Completed` / admin-only `Cancelled` tabs, signing/completion happens on `/p/sales-invoice-v2` in `packing-slip` mode, and Expo now exposes a separate mobile warehouse workspace at `/(drivers)/warehouse-packing` from Settings backed by `dispatch.packingList` with packing-aware detail routing into the existing dispatch detail screen; next slice is notification deep-link polish, broader browser/device validation, and finishing the new control-engine migration for packing/dispatch writes while cleaning remaining legacy status helpers that still mention `packing queue` (API + UI + Notifications + Print + Control)
- [x] Make old sales form updates refresh inventory line items and stock demand synchronously after save, matching the new sales form behavior.
# Sales Decimal/Rounding Corrections — implementation complete, deployment pending

- Shared decimal-safe calculations, legacy/new parity, grouped authoritative
  totals, percentage discounts, HPT final custom pricing, C.C.C separation, and
  shelf Decimal boundaries are implemented.
- Focused tests and isolated migration validation pass.
- Production/development database deployment of
  `20260720081100_sales_shelf_decimal_prices` remains an operator step because
  the ordinary development database has unrelated migration drift and was not
  reset.
