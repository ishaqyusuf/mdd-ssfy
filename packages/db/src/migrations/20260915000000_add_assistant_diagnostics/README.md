# Assistant diagnostics migration

Generated by Prisma 6.19.2 `migrate diff --from-empty --to-schema-datamodel
/tmp/gnd-assistant-diagnostics-schema.prisma --script`. The temporary schema
contained exact copies of `AssistantDiagnostic`, `AssistantDiagnosticReview` and
the repository's MySQL datasource (`relationMode = "prisma"`). Both models are
independent of the rest of the schema, except their mutual Prisma relation.

The SQL was regenerated and compared byte-for-byte. It creates only these two
tables and their indexes; it does not alter existing tables or business data.
No SQL was handwritten. The full repository migration command had already been
attempted and declined a reset due to unrelated shared-local migration drift.

The shared local QA schema already contains these tables from the earlier
repository `db:push`. This migration has not been deployed or marked applied.
Do not run its CREATE statements again on that database or mark it applied
without verifying the schema and following the repository's migration baseline
procedure. Preview/production rollout remains separate and must inspect each
target's migration history and schema first.
