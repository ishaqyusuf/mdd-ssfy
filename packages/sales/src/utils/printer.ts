interface PrintProps {}

export async function printSales(props: PrintProps) {
  //  const tok = await generateToken({
  //                         props.salesIds,
  //                         expiry: addDays(new Date(), 7).toISOString(),
  //                         mode: "order" as SalesPrintModes,
  //                     } satisfies SalesPdfToken);
  //                     openLink(
  //                         `/api/download/sales`,
  //                         {
  //                             token: tok,
  //                             preview: true,
  //                         },
  //                         true
  //                     );
}
