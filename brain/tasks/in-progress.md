# In Progress

## Purpose
Tracks the active work queue. Keep this focused and execution-ready.

## Current Focus
- [x] **Dead Code & Dead Link Cleanup 2026-04-10** — deleted 180 knip-identified unused files from `apps/www`; full dead-link analysis documented at `brain/decisions/dead-code-dead-links-cleanup-2026-04-10.md`; **action needed**: 7 broken nav links in `sidebar/links.ts` (dealers URL wrong, typo in quotes, missing commissions/pickups pages)
- [x] Sales PDF V2: isolated multi-template print system (`brain/features/sales-pdf-system.md`) — COMPLETED: Phase 4 (salesV2 tRPC endpoint), Phase 6 (print-sales-v2.tsx + /p/sales-invoice-v2 route), quick-print helper, sales-menu-print component
- [ ] Employee Management V2 — remaining phases: DB schema migration (`EmployeeRecord`), insurance gate on job creation, expo app mirror; shipped slices include `[employeeId]` overview route, `employees.route.ts` API layer, employee-specific permission overrides/counts, a jobs-dashboard insurance-gate freshness fix so submit CTAs wait for a fresh profile read instead of treating loading state as missing insurance, a dev-only web debug pill showing contractor custom-job activation beside the Tailwind breakpoint indicator, and a web job-form custom shortcut that appears in the project list and jumps directly to the final custom-job step
- [ ] Sales PDF V2 follow-up slice: wire quick-print entry points into sales form and sales overview, switch sales preview to the new template renderer, reduce print latency, and define stored-PDF cache invalidation/reuse rules including multi-print merge behavior (`brain/features/sales-pdf-system.md`)
- [ ] Sales form system hardening Phase 0: transactional save safety and server-authoritative pricing validation (`brain/sales-form-system-hardening-plan.md`) (Schema + API + Validation)
- [ ] Sales form system hardening Phase 1: pricing integrity fixes (labor writeback guard removal, taxable consistency, subtotal rendering correctness) with focused unit/integration coverage (API + UI + Validation)
- [ ] New sales form resilience hardening: default autosave back on, persist local recovery snapshots on page-leave, and warn before risky navigation when the draft is still dirty with autosave off / stale / error state (UI + Validation)
- [ ] Legacy sales-form mobile + architecture refactor: create `domains/sales-form/legacy`, centralize legacy classes/helpers behind adapters, flatten the shell UI, switch mobile to active-item editing with wrapped step CTA navigation, make HPT/moulding/service/shelf-items mobile-native, and extract save orchestration into application/server boundaries (`brain/legacy-sales-form-mobile-architecture-plan.md`) (Architecture + UI + API + Validation)
- [ ] Inventory demand/allocation workflow: manual allocation review and inbound issue handling are now layered onto the stock-monitored demand pipeline; `StockAllocation` suggestions now use `pending_review` until approved through the new `/inventory/allocations` queue, approved allocations are the only ones treated as committed stock, inbound receiving now captures `qtyGood` vs `qtyIssue`, open issues land on `InboundShipmentItemIssue`, and `/inventory/inbounds` can resolve issue rows inline; next slice is browser validation, safer repeat-receive guards, and deciding whether replacements should spawn linked follow-up inbounds automatically (Schema + Architecture + Inventory)
- [ ] Dyke-to-inventory domain migration: moved the active custom-component write path off `apps/www` server actions and into `@gnd/inventory` services exposed via inventories tRPC (`saveDykeStepComponent`, `updateDykeComponentPricing`), added async `sync-dyke-step-to-inventory` job wiring plus drift/repair endpoints, and switched the legacy sales-form custom component UI to call the new domain; next slice is migrating the remaining dormant Dyke admin/use-case helpers and deciding whether Dyke pricing should structurally sync into inventory pricing or remain sales-only (Architecture + API + Inventory)
- [ ] Supplier pricing integration for doors: introduced `Supplier` / `SupplierVariant` direction in inventory schema, added Dyke-supplier sync bridge by legacy UID, exposed supplier/supplier-variant mutations through inventories tRPC, added supplier management plus variant supplier-pricing editing inside the inventory form, added a dedicated `/inventory/suppliers` workspace with compact supplier items and search/create/edit/delete/default actions, and wired sales door pricing to prefer exact `SupplierVariant` matches while preserving legacy supplier dep-key buckets as fallback; next slice is backfilling safe door supplier pricing data into `SupplierVariant`, browser-validating the new suppliers workspace + default-supplier flow, and tightening preferred-supplier use in inbound/purchasing (Schema + API + UI + Inventory)
- [ ] Door supplier pricing canonicalization: main/full inventory import now auto-syncs Dyke suppliers before importing, both import strategies now parse supplier-specific Dyke pricing keys and upsert canonical `SupplierVariant` rows instead of keeping those prices only in legacy pricing buckets, the sales pricing resolver now expects supplier-specific prices to come from `SupplierVariant`, and the inventory supplier form now shows a Dyke-supplier matching review so UID mismatches are visible; next slice is browser validation plus a real-data import rerun to confirm supplier rows and unmatched review counts look correct (Import + Pricing + UI)
- [ ] Inventory vs component split: added `Inventory.productKind`, inventory/components list filtering, inventory-form type toggle, component-mode form hiding for variants/pricing/stock sections, importer classification defaults, a priced-item backfill mutation that marks anything with meaningful pricing as `inventory` and leaves unpriced Dyke/config rows as `component`, tightened storefront/receiving queries to exclude `component` rows by default, added a dedicated `/inventory/review` admin mismatch report, and simplified the inventory workspace shell so analytics live on `/inventory` only while subpages no longer inherit the old shared tab/dashboard layout; next slice is refining heuristics for edge cases and deciding whether any stockable-but-unpriced records should be force-kept as `inventory` via source-aware rules beyond pricing alone (Schema + API + UI + Import)
- [ ] Category-kind split: `InventoryCategory` now carries its own `productKind`, product forms request category lists by kind, sub-component selectors use component categories, sub-category selectors use inventory categories, and the categories workspace can switch between inventory and component categories without mixing the two sources; next slice is backfilling existing categories safely and reviewing whether any legacy mixed-use categories need manual reassignment before stricter cleanup rules land (Schema + API + UI)
- [ ] Inventory import control center: replaced the imports page’s table-first/per-category workflow with a global control-center direction that surfaces import analytics, system checks, full update/check/reset actions, and leaves the legacy category table as a secondary read-only diagnostic; import scope is now driven by the active sales-settings route graph plus dependency closure instead of all Dyke steps, targeted Dyke syncs now skip out-of-scope steps, the optimized importer now dedupes category-variant attributes, subcategory links, and image links against both existing DB state and in-flight rows, and the imports UI reports active/dependency/excluded/stale counts; full import actions now queue Trigger jobs instead of blocking synchronously, with the page tracking run status and refreshing after completion; next slice is adding richer import-run history/persistence, deeper health metrics, and a safe archive/cleanup path for stale imported categories after review (UI + Import + Operations)
- [ ] Inventory import source labeling: imported Dyke rows now preserve `sourceStepUid`, `sourceComponentUid`, and `sourceCustom`, inventory browsing excludes custom imports by default while keeping a `Show Custom` escape hatch, `backfillInventoryImportSources` can label existing imported rows retroactively, the imports control center now surfaces standard-vs-custom counts, and default operational queries (storefront, inbound matching, low-stock) now exclude custom imports; next slice is using those labels for deeper stale-import review, archive rules, and any explicit custom-inventory operational exceptions that should exist instead of leaking by default (Schema + API + UI + Import)
- [ ] Inventory app-domain split: moved the inventory App Router tree out of `apps/www/src/app/(sidebar)/(sales)/inventory` and into `apps/www/src/app/(sidebar)/inventory` so inventory now lives as its own workspace/domain in the web app while preserving the same `/inventory` URLs; next slice is removing any remaining “sales-owned” assumptions in shared labels, filters, and supporting modules where inventory still reads as a sales subfeature (Architecture + UI)
- [ ] New sales form parity hotfix/repro batch: close newly reported grouped-workflow and component-management gaps (`component edit`, `image attachment`, redirect route list accuracy, inline door base-cost edit, calculated sales-cost display, HPT add-size, HPT add-door option, Door Size Variant strict filtering/runtime proof, moulding outside-click close) and refresh Phase 0 evidence for all 24 rows (API + UI + Validation)
- [ ] Sales Orders V2: build the clean `sales-book` orders workspace on dedicated v2 query/filter contracts, summary widgets, reusable invoice-style table shell, and unified funnel status system (`brain/features/sales-orders-v2.md`)
- [ ] Pickup packing funnel: `Send for Pickup` now routes packing work into queued pickup deliveries with `sales-packing-list` notification membership, the canonical warehouse page lives at `/sales/packing-list` under the sidebar tree with `Current` / `Completed` / admin-only `Cancelled` tabs, and signing/completion now happens on `/p/sales-invoice-v2` in `packing-slip` mode; next slice is finishing the new control-engine migration for packing/dispatch writes and cleaning remaining legacy status helpers that still mention `packing queue` (API + UI + Notifications + Print + Control)
- [x] Make old sales form updates refresh inventory line items and stock demand synchronously after save, matching the new sales form behavior.
